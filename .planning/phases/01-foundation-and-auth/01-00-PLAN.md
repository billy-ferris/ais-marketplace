---
phase: 01-foundation-and-auth
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/vitest.config.ts
  - apps/api/src/__tests__/setup.ts
  - apps/api/src/__tests__/schema.test.ts
  - apps/api/src/__tests__/rbac.test.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-06

must_haves:
  truths:
    - "vitest is installed as a dev dependency in apps/api"
    - "vitest.config.ts exists and runs tests from src/__tests__/"
    - "pnpm --filter @ais/api test executes and reports zero tests (or stub tests) without errors"
    - "Test stub files exist for schema validation and RBAC middleware"
  artifacts:
    - path: "apps/api/vitest.config.ts"
      provides: "Vitest configuration for the API package"
      contains: "defineConfig"
    - path: "apps/api/src/__tests__/setup.ts"
      provides: "Test setup file for shared test utilities"
    - path: "apps/api/src/__tests__/schema.test.ts"
      provides: "Stub test file for database schema validation"
      contains: "describe"
    - path: "apps/api/src/__tests__/rbac.test.ts"
      provides: "Stub test file for RBAC middleware validation"
      contains: "describe"
  key_links:
    - from: "apps/api/vitest.config.ts"
      to: "apps/api/src/__tests__/"
      via: "include glob pattern"
      pattern: "include.*__tests__"
---

<objective>
Install vitest and create the test infrastructure needed for Nyquist-compliant validation sampling throughout Phase 1.

Purpose: VALIDATION.md declares wave_0_complete: false and lists vitest installation, vitest.config.ts, and test directories as Wave 0 requirements. Without this infrastructure, no automated test verification can run during execution of Plans 01-04.

Output: Working vitest setup in apps/api with stub test files that subsequent plans will populate with real assertions.
</objective>

<execution_context>
@/Users/billy.ferris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billy.ferris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create test configuration</name>
  <files>apps/api/vitest.config.ts, apps/api/src/__tests__/setup.ts</files>
  <action>
Set up vitest in the API package so that `pnpm --filter @ais/api test` works.

1. **Install vitest** in apps/api as a dev dependency. It may already be listed in apps/api/package.json from Plan 01 scaffolding — if so, just ensure it is installed. If not, add it:
   ```bash
   cd apps/api && pnpm add -D vitest
   ```

2. **apps/api/vitest.config.ts**:
   ```typescript
   import { defineConfig } from 'vitest/config';

   export default defineConfig({
     test: {
       include: ['src/**/*.test.ts'],
       setupFiles: ['src/__tests__/setup.ts'],
       environment: 'node',
     },
   });
   ```

3. **apps/api/src/__tests__/setup.ts**:
   ```typescript
   // Shared test setup for @ais/api tests
   // Add global test utilities, mocks, or environment setup here
   ```

4. **Ensure apps/api/package.json** has a `"test"` script:
   - `"test": "vitest run"` (NOT `vitest` without `run` — we want single-run, not watch mode)
   - If the script already exists, verify it uses `vitest run` not watch mode.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm --filter @ais/api test 2>&1 | tail -5</automated>
  </verify>
  <done>vitest is installed, vitest.config.ts exists, and `pnpm --filter @ais/api test` runs without errors (reporting 0 tests found or a passing stub).</done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for schema and RBAC validation</name>
  <files>apps/api/src/__tests__/schema.test.ts, apps/api/src/__tests__/rbac.test.ts</files>
  <action>
Create stub test files that Plans 02 and 04 will populate with real assertions. These stubs ensure the test infrastructure is wired correctly and provide a skeleton for later implementation.

1. **apps/api/src/__tests__/schema.test.ts**:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('Database Schema', () => {
     describe('companies table', () => {
       it.todo('should define all required columns');
       it.todo('should use pgEnum for company type');
       it.todo('should default marginPercentage to 10.00');
     });

     describe('users table', () => {
       it.todo('should define all required columns');
       it.todo('should have unique constraint on clerkId');
       it.todo('should have unique constraint on email');
       it.todo('should reference companies.id via foreign key');
       it.todo('should use pgEnum for user role');
     });
   });
   ```

2. **apps/api/src/__tests__/rbac.test.ts**:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('RBAC Middleware', () => {
     describe('requireRole', () => {
       it.todo('should allow access when user has the required role');
       it.todo('should return 403 when user lacks the required role');
       it.todo('should return 401 when no auth session exists');
     });

     describe('requireAuth', () => {
       it.todo('should allow authenticated requests');
       it.todo('should reject unauthenticated requests');
     });
   });
   ```
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm --filter @ais/api test 2>&1 | tail -10</automated>
  </verify>
  <done>Stub test files exist in apps/api/src/__tests__/. Running `pnpm --filter @ais/api test` finds the test files and reports todo tests without errors. Wave 0 is complete.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ais/api test` runs without errors
- vitest.config.ts uses `vitest run` (not watch mode)
- Test stubs exist for schema validation and RBAC middleware
- Test directory structure matches VALIDATION.md requirements
</verification>

<success_criteria>
- vitest installed and configured in apps/api
- `pnpm --filter @ais/api test` executes successfully
- Stub test files provide skeleton for Plans 02 and 04 to fill in
- VALIDATION.md wave_0_complete can be set to true after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-00-SUMMARY.md`
</output>
