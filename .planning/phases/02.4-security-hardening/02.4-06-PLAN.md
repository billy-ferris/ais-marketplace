---
phase: 02.4-security-hardening
plan: 06
type: execute
wave: 3
depends_on: ["02.4-04", "02.4-05"]
files_modified: []
autonomous: false
requirements: [SEC-AUTHZ, SEC-DATA-INTEGRITY]
must_haves:
  truths:
    - "DB-level constraints (slug reuse-after-delete, one-primary-per-listing, FK onDelete) behave as declared against a live Postgres"
    - "Client role gating denies retailers and scopes manufacturer/admin correctly in the browser"
    - "End-to-end upload enforces content-type and 5 MB size server-side"
  artifacts: []
  key_links: []
---

<objective>
Final manual UAT gate for the fixes that cannot be covered by the apps/api Vitest harness: DB-level constraints (D-09/D-10/D-13), client role gating (D-04/D-05), and end-to-end upload validation (D-06/D-07). This plan runs after the schema push (Plan 04) and the client changes (Plan 05).

Purpose: apps/web has no test harness and DB constraints are not exercisable in the mocked-db unit harness. These behaviors must be human-verified before `/gsd:verify-work`.

Output: A recorded pass/fail for each manual check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.4-security-hardening/02.4-CONTEXT.md
@.planning/phases/02.4-security-hardening/02.4-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT 1: Verify DB-level data-integrity constraints (D-09/D-10/D-13)</name>
  <action>Human runs the DB-level checks below against the live dev Postgres after Plan 04's db:push. This is a verification-only checkpoint — no code is written.</action>
  <what-built>DB-level data-integrity constraints pushed to the dev Postgres (Plan 04): slug partial unique index WHERE deleted_at IS NULL (brands + categories), one-primary-per-listing partial unique index (brand_listing_images, after dedup), and explicit FK onDelete policies.</what-built>
  <how-to-verify>
    1. Slug reuse (D-09): soft-delete a brand (set deleted_at), then create a NEW brand with the SAME slug -> succeeds. Then attempt a SECOND live brand with an already-live slug -> rejected by the DB. Repeat the live-duplicate check for categories.
    2. One-primary (D-10): for a single listing, attempt to set two brand_listing_images rows to is_primary=true -> the second is rejected by `brand_listing_images_one_primary_per_listing`.
    3. FK onDelete (D-13): confirm the pushed policy matches Plan 04's recorded decision — restrict on brands.companyId and listingCategories.categoryId; cascade on inventorySkus.listingId, brandListingImages.listingId, listingCategories.listingId. (A hard-delete of a listing cascades its skus/images/category rows; a hard-delete of a referenced company/category is blocked.)
  </how-to-verify>
  <acceptance_criteria>
    - Slug reusable after soft-delete; live duplicate rejected (brands + categories)
    - Second is_primary=true per listing rejected
    - FK onDelete behavior matches the recorded D-13 policy
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which DB check failed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT 2: Verify client role gating in the browser (D-04/D-05)</name>
  <action>Human logs in as each role and confirms the /manage/* route gating below. Verification-only checkpoint — no code is written.</action>
  <what-built>Client role gating (Plan 05, D-04/D-05): RoleGuard wraps every /manage/* route with ForbiddenPage as the deny UX.</what-built>
  <how-to-verify>
    Using the seeded accounts (admin@ais-demo.com and the manufacturer/retailer logins, password AisMarket2026!):
    1. Log in as RETAILER -> navigate to /manage/brands, /manage/listings, /manage/categories, /manage/approvals -> each shows ForbiddenPage (no redirect).
    2. Log in as MANUFACTURER -> /manage/brands and /manage/listings/* are reachable; /manage/categories and /manage/approvals/* show ForbiddenPage.
    3. Log in as ADMIN -> all /manage/* routes reachable.
  </how-to-verify>
  <acceptance_criteria>
    - Retailer denied on all /manage/*
    - Manufacturer allowed on brands/listings, denied on categories/approvals
    - Admin allowed everywhere
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which route gate failed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT 3: Verify end-to-end upload validation (D-06/D-07)</name>
  <action>Human exercises the upload flow below (after confirming R2 CORS allows POST) to confirm server-side content-type and size enforcement. Verification-only checkpoint — no code is written.</action>
  <what-built>End-to-end upload validation (Plans 03 + 05, D-06/D-07): server presign allowlists image/jpeg|png|webp and signs a 5 MB content-length-range; client uploads via multipart POST and checks uploadRes.ok.</what-built>
  <how-to-verify>
    Prerequisite: confirm the R2 bucket CORS policy allows the POST method (see Plan 03 user_setup — was configured for PUT).
    1. Upload a valid JPEG/PNG/WebP under 5 MB via the ImageUploader -> succeeds and the image renders.
    2. Attempt an SVG -> rejected (server 400 "Unsupported content type").
    3. Attempt a file larger than 5 MB -> rejected server-side by the content-length-range policy.
    4. Confirm a failed upload does NOT leave a broken publicUrl (the hook throws on !uploadRes.ok).
  </how-to-verify>
  <acceptance_criteria>
    - Valid image under 5 MB uploads successfully via multipart POST
    - SVG and other non-allowlisted types rejected server-side
    - Files over 5 MB rejected server-side
    - Failed uploads surface an error rather than a broken publicUrl
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which upload check failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification -> deployed behavior | This plan asserts the DB, client, and upload boundaries from Plans 03/04/05 actually hold at runtime before the phase gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.4-14 | Repudiation / Integrity | unverified DB/client/upload mitigations shipping unproven | mitigate | Blocking human UAT confirms D-09/10/13, D-04/05, D-06/07 behave as declared before /gsd:verify-work |
</threat_model>

<verification>
- All three UAT checkpoints approved
- Full apps/api suite green (from Plans 01-03) as the automated companion to this manual gate
</verification>

<success_criteria>
- DB constraints, client role gating, and upload validation all confirmed by human verification
</success_criteria>

<output>
Create `.planning/phases/02.4-security-hardening/02.4-06-SUMMARY.md` when done, recording the pass/fail of each manual check
</output>
