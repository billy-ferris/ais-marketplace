---
phase: 02.4
slug: security-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-01
---

# Phase 02.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (apps/api only — apps/web has NO test harness) |
| **Config file** | apps/api (vitest, mocked-db harness) |
| **Quick run command** | `pnpm --filter @ais/api test` |
| **Full suite command** | `pnpm --filter @ais/api test` |
| **Estimated runtime** | ~seconds (mocked db) |

---

## Sampling Rate

- **After every task commit:** Run the quick command for touched API area
- **After every plan wave:** Run the full API suite
- **Before `/gsd:verify-work`:** Full API suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

*Populated by the planner. Handler-level fixes are unit-testable in the Vitest mocked-db harness; DB-constraint and client fixes are manual-UAT only (see below).*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | — | SEC-* | — | — | unit/manual | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Populated by the planner. Research flagged an explicit Wave 0 test-gap list for the handler-level fixes.*

- [ ] API test stubs for authz fixes (users/companies read scope, PATCH brandId cross-tenant)
- [ ] API test stubs for injection escaping (email HTML/CRLF)
- [ ] API test stub for seed production guard (`assertNotProduction()` extracted-pure)
- [ ] API test stub for upload content-type/size rejection

---

## Manual-Only Verifications

Per research: `apps/web` has no test harness, and DB-constraint fixes are DB-level.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Slug partial unique index (WHERE deleted_at IS NULL) | SEC-DATA-INTEGRITY | DB constraint, not handler logic | Soft-delete a brand, recreate with same slug — succeeds; duplicate live slug — rejected |
| One-primary-image partial index (after dedup migration) | SEC-DATA-INTEGRITY | DB constraint | Attempt two is_primary=true rows for one listing — rejected |
| FK onDelete behavior | SEC-DATA-INTEGRITY | DB constraint | Delete parent, observe declared onDelete effect |
| RoleGuard router wiring (route→role map) | SEC-AUTHZ | apps/web has no test harness | Log in as retailer, hit /manage/* — ForbiddenPage; manufacturer hits brands/listings — allowed |
| Presigned upload size/type enforcement (PUT→POST) | SEC-INJECTION / SEC-DATA-INTEGRITY | R2 config + client-side; needs live bucket | Upload >5MB or SVG — rejected server-side |

---

## Validation Sign-Off

- [ ] All handler tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
