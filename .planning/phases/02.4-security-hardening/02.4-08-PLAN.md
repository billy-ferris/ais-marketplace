---
phase: 02.4-security-hardening
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - apps/web/src/components/shared/ImageUploader.tsx
  - apps/web/src/components/manage/BrandDialog.tsx
  - apps/web/src/components/manage/ListingForm.tsx
  - apps/web/src/components/manage/SkuInlineEditor.tsx
autonomous: true
requirements: [SEC-DATA-INTEGRITY]

must_haves:
  truths:
    - "Selecting a server-rejected file (e.g. SVG → 400) shows a visible error in the uploader UI, not only the console"
    - "Any thrown ApiError / upload failure from useUpload().uploadFile is rendered to the user in the destructive text slot"
    - "The existing client-side size pre-check error still renders (no regression)"
  artifacts:
    - path: "apps/web/src/components/shared/ImageUploader.tsx"
      provides: "External error prop rendered in the existing text-destructive slot"
      contains: "error"
    - path: "apps/web/src/components/manage/BrandDialog.tsx"
      provides: "Catches uploadFile rejection and passes the message to ImageUploader"
      contains: "getApiErrorMessage"
    - path: "apps/web/src/components/manage/ListingForm.tsx"
      provides: "Catches uploadFile rejection and passes the message to ImageUploader"
      contains: "getApiErrorMessage"
    - path: "apps/web/src/components/manage/SkuInlineEditor.tsx"
      provides: "Surfaces per-cell upload rejection into the visible cell error"
      contains: "getApiErrorMessage"
  key_links:
    - from: "apps/web/src/components/manage/BrandDialog.tsx"
      to: "apps/web/src/components/shared/ImageUploader.tsx"
      via: "error prop"
      pattern: "error="
    - from: "apps/web/src/components/manage/ListingForm.tsx"
      to: "apps/web/src/components/shared/ImageUploader.tsx"
      via: "error prop"
      pattern: "error="
---

<objective>
Close Gap 2 from 02.4-06-UAT.md: the server correctly rejects disallowed uploads (D-06 SVG → 400) but the failure is swallowed — `ImageUploader` only renders its own client-side size pre-check error, and the three consuming forms catch `uploadFile`'s rejection into empty `catch {}` blocks that log to console only. Give `ImageUploader` an external error path and wire each consuming form to surface the rejection message.

Purpose: A user who picks a rejected file sees why, instead of a silent no-op.
Output: `ImageUploader` `error` prop rendered in the existing destructive slot; BrandDialog, ListingForm, and SkuInlineEditor surface upload rejections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.4-security-hardening/02.4-06-UAT.md
@.planning/phases/02.4-security-hardening/02.4-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Do not explore the codebase for these. -->

Existing error helper — apps/web/src/lib/api.ts:
  export class ApiError extends Error { ... }
  export function getApiErrorMessage(error: unknown, fallback: string): string
  // Resolves the most specific human-readable message from an ApiError/Error; use it in every catch.

ImageUploader current props — apps/web/src/components/shared/ImageUploader.tsx:
  interface ImageUploaderProps {
    value: string | null;
    onChange: (file: File) => void;
    onRemove?: () => void;
    accept?: string;
    maxSizeMB?: number;   // default 5
    disabled?: boolean;
    isUploading?: boolean;
    className?: string;
  }
  // Internal `error` state + a text-destructive <p> already render the client size pre-check message.
  // ALLOWED (mirror D-06 for optional client pre-check): image/jpeg, image/png, image/webp

Upload hook (stable public API from Plan 07) — apps/web/src/hooks/useUpload.ts:
  useUpload(): { uploadFile: (file: File) => Promise<string>, isUploading: boolean }
  // uploadFile REJECTS (throws ApiError on presign 400, or Error on a failed PUT).

Consuming forms and their upload handlers:
  BrandDialog.tsx      → handleImageUpload(file) { try { const url = await uploadFile(file); setLogoUrl(url); setValue('logoUrl', url) } catch {} }  → <ImageUploader ... />
  ListingForm.tsx      → handleImageUpload(file) { try { const url = await uploadFile(file); setFormImages(...) } catch {} }                          → <ImageUploader ... />
  SkuInlineEditor.tsx  → handleImageUpload(index, file) { try { const url = await uploadFile(file); handleFieldChange(index,'imageUrl',url) } catch {} }
                          // NOTE: SkuInlineEditor uses a raw <input type="file"> cell, NOT ImageUploader.
                          // It already has a per-cell error map `skuErrors` keyed `${index}:${field}` rendered via renderRequiredCell tooltips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an external error prop to ImageUploader and render it in the existing destructive slot</name>
  <files>apps/web/src/components/shared/ImageUploader.tsx</files>
  <read_first>
    - apps/web/src/components/shared/ImageUploader.tsx (full component; note the internal `error` useState and the existing text-destructive <p>)
    - apps/web/src/lib/api.ts (getApiErrorMessage / ApiError — for the mirrored client pre-check message wording)
  </read_first>
  <action>
    Add an optional `error?: string` prop to `ImageUploaderProps` and accept it in the destructured props.

    Render the external error together with the existing internal client-side pre-check error: compute a single `displayError = error ?? internalError` (keep the existing `useState` for the size pre-check) and render `displayError` in the existing `text-destructive` <p> (show the <p> when displayError is truthy). Do not remove the internal size pre-check — it must still render when no external error is set.

    Add an optional client-side content-type pre-check that mirrors the D-06 server allowlist (image/jpeg, image/png, image/webp) inside `handleFile`: if `file.type` is not in the allowlist, `setError` to a clear message (e.g. "Only JPEG, PNG, or WebP images are allowed") and return without calling `onChange` — this gives faster feedback but is UX-only; the server remains authoritative. Keep the existing size pre-check ordering.

    Do NOT add tests (apps/web has no harness). Do not change unrelated props or the preview/drag behavior.
  </action>
  <verify>
    <automated>pnpm --filter @ais/web build</automated>
    grep -n "error?: string" apps/web/src/components/shared/ImageUploader.tsx
  </verify>
  <acceptance_criteria>
    - ImageUploaderProps has an optional `error?: string`; the destructive <p> renders an external error OR the internal pre-check error
    - Client content-type pre-check rejects non-jpeg/png/webp before calling onChange (mirrors D-06, UX-only)
    - No regression to the existing size pre-check rendering
    - `pnpm --filter @ais/web build` (tsc) passes
  </acceptance_criteria>
  <done>ImageUploader renders an external error prop in its destructive slot without regressing the internal pre-check.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three consuming forms to catch uploadFile rejections and surface the message</name>
  <files>apps/web/src/components/manage/BrandDialog.tsx, apps/web/src/components/manage/ListingForm.tsx, apps/web/src/components/manage/SkuInlineEditor.tsx</files>
  <read_first>
    - apps/web/src/components/manage/BrandDialog.tsx (handleImageUpload ~L114 with empty catch; ImageUploader render ~L242)
    - apps/web/src/components/manage/ListingForm.tsx (handleImageUpload ~L199 with empty catch; ImageUploader render ~L519)
    - apps/web/src/components/manage/SkuInlineEditor.tsx (handleImageUpload ~L258 with empty catch; skuErrors map + renderRequiredCell tooltip rendering; raw file input cell)
    - apps/web/src/lib/api.ts (getApiErrorMessage)
  </read_first>
  <action>
    BrandDialog.tsx and ListingForm.tsx (both render ImageUploader):
    - Add a local `uploadError` state (`useState<string | null>(null)`).
    - At the start of `handleImageUpload`, clear it (`setUploadError(null)`).
    - Replace the empty `catch {}` with `catch (err) { setUploadError(getApiErrorMessage(err, 'Upload failed')) }` — import `getApiErrorMessage` from '@/lib/api'.
    - Pass `error={uploadError ?? undefined}` to the `<ImageUploader>` render so the message shows in its destructive slot.

    SkuInlineEditor.tsx (raw file-input cell, no ImageUploader — reuse its existing per-cell error surface):
    - Import `getApiErrorMessage` from '@/lib/api'.
    - In `handleImageUpload(index, file)`, replace the empty `catch {}` with a catch that writes the resolved message into the existing per-cell error map for that row's image cell (use the same `${index}:...` keying the component already uses for `skuErrors`, e.g. `${index}:imageUrl`) via the setter the component uses (`setSkuErrors`), and clear that key at the start of the handler. This makes the upload rejection render through the existing renderRequiredCell/tooltip path for that cell. If the image cell is not covered by renderRequiredCell, render the message inline next to the file-input cell in the existing `text-destructive` style instead.

    Use `getApiErrorMessage` (not a bare `err.message`) so ApiError form/field messages resolve correctly. Do not add tests. Do not alter unrelated form logic.
  </action>
  <verify>
    <automated>pnpm --filter @ais/web build</automated>
    grep -c "getApiErrorMessage" apps/web/src/components/manage/BrandDialog.tsx apps/web/src/components/manage/ListingForm.tsx apps/web/src/components/manage/SkuInlineEditor.tsx
  </verify>
  <acceptance_criteria>
    - BrandDialog and ListingForm hold an uploadError state, set it in catch via getApiErrorMessage, and pass `error` to ImageUploader
    - SkuInlineEditor surfaces the upload rejection into a visible per-cell error (not an empty catch)
    - No empty `catch {}` remains around an `await uploadFile(...)` call in any of the three forms
    - `pnpm --filter @ais/web build` (tsc) passes
    - Acceptance of the visible-error behavior is re-running Plan 06 UAT 3 (manual — see verification)
  </acceptance_criteria>
  <done>All three forms surface uploadFile rejections via getApiErrorMessage; no empty catch remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → API presign endpoint | The server-side D-06/D-07 controls (Plan 07) are authoritative; this plan only makes their rejections VISIBLE and adds a UX-only client pre-check |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-024-05 | Repudiation/Info Disclosure | ImageUploader + consuming forms | mitigate | Surface server rejections (D-06 content-type, D-07 size) to the user so a blocked upload is not silently dropped; message resolved via getApiErrorMessage without leaking internals |
| T-024-06 | Elevation (client bypass) | ImageUploader client content-type pre-check | accept | Client allowlist mirrors D-06 for fast feedback only; it is not a security boundary — the server presign (Plan 07) still rejects. Bypassing the client check changes nothing server-side |
</threat_model>

<verification>
- `pnpm --filter @ais/web build` passes (apps/web has no test harness — no UI tests, per project-wide practice).
- **Acceptance gate = re-run Plan 06 UAT 3 (manual):** select an SVG and confirm a visible error appears in the uploader (not just the console); confirm a >5 MB or otherwise-rejected upload shows a message; confirm the existing size pre-check still renders. Do NOT create a new manual-UAT plan.
</verification>

<success_criteria>
- ImageUploader renders an external `error` prop in its destructive slot without regressing the internal pre-check.
- All three consuming forms surface uploadFile rejections to the user via getApiErrorMessage.
- No empty catch swallows an upload error.
</success_criteria>

<output>
Create `.planning/phases/02.4-security-hardening/02.4-08-SUMMARY.md` when done.
</output>
