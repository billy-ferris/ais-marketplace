---
phase: 02.3-form-input-validation
verified: 2026-07-01T02:39:23Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
---

# Phase 02.3: Form Input Validation Verification Report

**Phase Goal:** All four manage forms (ListingForm, BrandDialog, CategoryDialog, SkuInlineEditor) provide consistent client-side validation UX — validate on blur, clear on valid change, catch-all with focus on submit, required-field asterisks, textarea character counters, and SKU per-cell validation — backed by a restructured Zod flatten()-shaped API error contract and a typed ApiError on the client.
**Verified:** 2026-07-01T02:39:23Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A failed Zod validation on any API route returns HTTP 400 with `{ error, fieldErrors, formErrors }` | ✓ VERIFIED | `apps/api/src/middleware/error.ts:7-11` calls `err.flatten()`; `error.test.ts` asserts exact shape and absence of `details` key. Independently re-ran `pnpm --filter @ais/api test` → 138/138 passed. |
| 2 | `fieldErrors` is an object of string arrays (Zod flatten() native shape) | ✓ VERIFIED | `error.test.ts:44-59,61-71` asserts `Array.isArray(fieldErrors.name)`, string elements, and exact equality with `err.flatten()`. |
| 3 | `apiFetch` throws a typed `ApiError` carrying status/fieldErrors/formErrors | ✓ VERIFIED | `apps/web/src/lib/api.ts:9-26` exports `class ApiError extends Error`; `apiFetch` (lines 72-84) parses body once and throws `new ApiError(message, status, fieldErrors, formErrors)`. |
| 4 | A failed mutation surfaces the real server message in a toast (not generic "API error: 400") | ✓ VERIFIED | Code review found this broken as originally shipped (WR-01: `formErrors` empty for single-field errors, dead-code fallback). Fixed in commit `cfdc677` — `getApiErrorMessage()` (api.ts:38-47) now falls back to first `fieldErrors` value; all three hook files (`useBrands.ts`, `useCategories.ts`, `useListings.ts`) call it in every `onError`. Independently confirmed present in current source. |
| 5 | Leaving a required text field empty shows an inline error on blur (all 3 RHF forms) | ✓ VERIFIED | `mode: 'onBlur'` present in `ListingForm.tsx:135`, `BrandDialog.tsx:68`, `CategoryDialog.tsx:45`; `<FieldError>` renders `errors.x?.message`. |
| 6 | An error clears the instant the field becomes valid while typing | ✓ VERIFIED | `reValidateMode: 'onChange'` set alongside `mode: 'onBlur'` in all three useForm calls. |
| 7 | Submitting a form with untouched empty required fields shows errors and scrolls/focuses first invalid field | ✓ VERIFIED | No `shouldFocusError: false` override found anywhere in `apps/web/src/components/manage/` (RHF default `shouldFocusError: true` applies); confirmed working in UAT (02.3-04-SUMMARY flow 1-3). |
| 8 | Every required field label shows a red asterisk; optional fields do not | ✓ VERIFIED | `RequiredLabel required` used for: CategoryDialog name, BrandDialog name + Manufacturer, ListingForm name + Brand. Plain `Label` (no asterisk) used for brand description, category icon/displayOrder, listing status/categories — matches the documented intentional exclusions. |
| 9 | Textarea fields (listing description, brand description) show a live character counter x/max | ✓ VERIFIED | `CharCounter value={watch('description') ?? ''} max={2000}` in BrandDialog.tsx:196; `max={5000}` in ListingForm.tsx:358. CategoryDialog has no textarea, correctly has no counter. |
| 10 | Required select fields (brand, manufacturer) show an inline error on blur when left empty | ✓ VERIFIED | `onOpenChange` handlers call `trigger('brandId')` (ListingForm.tsx:369-373) and set a manual `companyId` error on close (BrandDialog.tsx:207-216) — the latter compensates for `companyId` being `.optional()` at the schema level (see CR-02 below). |
| 11 | Blurring an invalid SKU cell marks it invalid with a red border | ✓ VERIFIED | `aria-invalid={!!skuErrors[key]}` set on every validated cell in `SkuInlineEditor.tsx`; `onBlur={() => validateSkuField(...)}` wired on name/sku/upc/size/casePack/casesPerPallet/price/msrp/quantity (all 9 schema fields, not just the original 4 — see CR-01 below). |
| 12 | Hovering an invalid SKU cell shows a tooltip with the validation message | ✓ VERIFIED | `renderRequiredCell()` wraps invalid inputs in `<Tooltip><TooltipTrigger render={input}/><TooltipContent>{message}</TooltipContent></Tooltip>`; `TooltipProvider` mounted once at component root (line 292). |
| 13 | A cell's error clears once its value becomes valid | ✓ VERIFIED | Each `onChange` handler re-runs `validateSkuField` only when that cell currently has an error (mirrors `reValidateMode: onChange`). |
| 14 | Rows marked for deletion (`_deleted`) are never flagged as invalid | ✓ VERIFIED | `validateAllRows` (line 233) does `if (row._deleted) return;`; deleted rows render in a separate branch (lines 301-321) with no validated `Input`s at all. |
| 15 | Price and MSRP are validated on blur only (no keystroke restriction) | ✓ VERIFIED | Price/MSRP inputs (lines 479-517) have no numeric `onChange` filtering — free-text `onChange` plus `onBlur` validation only. |
| 16 | Submitting a listing with an untouched invalid/empty SKU row is blocked and offending cells show errors (submit-time catch-all) | ✓ VERIFIED | `ListingForm.onFormSubmit` (lines 262-274) calls `skuEditorRef.current?.validateAll()` before building the payload and `return`s early (plus a toast) when false. Code review (CR-01) found the original implementation only surfaced errors for 4 of 9 validated fields, silently blocking Save with zero visible error for casePack/casesPerPallet/sku/upc/size — fixed in commit `d5106d7`; `SKU_VALIDATED_FIELDS` now covers all 9 fields and every failing field gets a message. |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/src/middleware/error.ts` | ZodError → flatten() 400 body | ✓ VERIFIED | Contains `err.flatten()`; also hardened (WR-03, commit `0b0ab07`) against non-object `err` and invalid status codes. |
| `apps/api/src/__tests__/middleware/error.test.ts` | Automated contract test | ✓ VERIFIED | 5 tests, all passing independently re-run. |
| `apps/web/src/lib/api.ts` | `ApiError` class + structured parsing | ✓ VERIFIED | Exports `ApiError`, `apiFetch`, `getApiErrorMessage`. |
| `apps/web/src/components/manage/FormFieldHelpers.tsx` | `RequiredLabel`, `FieldError`, `CharCounter` | ✓ VERIFIED | All three exported, stateless, matches spec (63 lines, well above min_lines: 20). |
| `apps/web/src/components/manage/ListingForm.tsx` | Timing + indicators + SKU submit gate | ✓ VERIFIED | `mode: 'onBlur'` present; SKU gate wired. |
| `apps/web/src/components/manage/BrandDialog.tsx` | Timing + indicators + manufacturer required gate | ✓ VERIFIED | `mode: 'onBlur'` present; client-side admin gate added (CR-02). |
| `apps/web/src/components/manage/CategoryDialog.tsx` | Timing + name asterisk | ✓ VERIFIED | `mode: 'onBlur'` present; displayOrder clearing bug fixed (WR-04). |
| `apps/web/src/components/manage/SkuInlineEditor.tsx` | Per-cell blur validation + `validateAllRows` + tooltip | ✓ VERIFIED | Contains `safeParse`, `TooltipProvider` mounted, `forwardRef`/`useImperativeHandle` exposing `validateAll`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `apps/web/src/lib/api.ts` | `apps/api/src/middleware/error.ts` | HTTP error body contract `{ error, fieldErrors, formErrors }` | ✓ WIRED | Client reads flat `body?.error`, `body?.fieldErrors`, `body?.formErrors` matching server shape exactly. |
| `apps/web/src/hooks/useBrands.ts` (+ useCategories, useListings) | `apps/web/src/lib/api.ts` | `getApiErrorMessage` / `instanceof ApiError` | ✓ WIRED | All 14 mutation `onError` handlers call `getApiErrorMessage(error, fallback)`. |
| `apps/web/src/components/manage/ListingForm.tsx` | `apps/web/src/components/manage/FormFieldHelpers.tsx` | import `RequiredLabel`/`FieldError`/`CharCounter` | ✓ WIRED | Imported and used for name/description/brand. |
| `apps/web/src/components/manage/BrandDialog.tsx` | react-hook-form `trigger()` | select `onOpenChange` → `trigger`/manual `setError` | ✓ WIRED | `companyId` gets a manual error on close when unselected (compensates for optional schema — see CR-02). |
| `apps/web/src/components/manage/SkuInlineEditor.tsx` | `packages/shared/src/schemas/inventory-sku.ts` | `createSkuSchema.safeParse` per row on blur and submit | ✓ WIRED | Confirmed schema shape (`casePack`/`casesPerPallet` `.positive()`, `price`/`msrp` regex, `quantity` nonnegative int) matches validation logic. |
| `apps/web/src/components/manage/SkuInlineEditor.tsx` | `apps/web/src/components/ui/tooltip.tsx` | `TooltipProvider` mounted + `Tooltip`/`TooltipTrigger(render)`/`TooltipContent` | ✓ WIRED | Provider wraps the full returned tree (was previously exported but never mounted anywhere in the app — RESEARCH Pitfall 5). |
| `apps/web/src/components/manage/ListingForm.tsx` | `apps/web/src/components/manage/SkuInlineEditor.tsx` | ref imperative handle `validateAll()` in `onFormSubmit` | ✓ WIRED | `skuEditorRef.current?.validateAll()` called before payload assembly; blocks submit + toasts on failure. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| API error-contract test suite | `pnpm --filter @ais/api test` | 138 passed, 0 failed (11 test files) | ✓ PASS |
| Web type-check | `pnpm --filter @ais/web exec tsc --noEmit` | exit 0, no errors | ✓ PASS |
| Full monorepo build | `pnpm build` | 2/2 tasks successful (`@ais/web`, `@ais/api`) | ✓ PASS |
| Debt-marker scan (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) across all 11 phase-modified files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` | No matches | ✓ PASS |
| Server-side admin `companyId` enforcement (backstops the client-only CR-02 gate) | `grep -n companyId apps/api/src/routes/brands.ts` | Route rejects missing `companyId` for admin path (`res.status(400).json({ error: 'companyId is required' })`, line 195) | ✓ PASS |

### Code Review Findings — Independently Re-Verified

A standalone code review (`02.3-REVIEW.md`) found 2 Critical + 6 Warning issues after initial implementation. All 8 were fixed in dedicated commits; I independently re-read the current source (not the review's claims) and confirm each fix is present and correct:

| ID | Issue | Fix Commit | Verified in Current Source |
|----|-------|-----------|------------------------------|
| CR-01 | SKU submit catch-all silently blocked Save with zero visible error for casePack/casesPerPallet/sku/upc/size | `d5106d7` | Confirmed — `SKU_VALIDATED_FIELDS` now covers all 9 schema fields; every field gets `onBlur` + tooltip wiring; submit failure also shows a toast. |
| CR-02 | "Manufacturer" shown as required in UI but `createBrandSchema.companyId` is `.optional()`, so never enforced | `19be7ff` | Confirmed — `BrandDialog.onSubmit` (lines 129-141) manually calls `setError('companyId', ...)` for the non-manufacturer path when `companyId` is falsy; server route independently enforces the same rule (`brands.ts:194-198`). |
| WR-01 | Real-server-message toast only worked for whole-object `.refine()` failures; single required-field errors always showed generic "Validation failed" | `cfdc677` | Confirmed — `getApiErrorMessage()` falls back to `Object.values(error.fieldErrors)[0]?.[0]`. |
| WR-02 | Removing an unsaved SKU row misattributed error state to the wrong row (index shift not reconciled) | `d39ef8d` | Confirmed — `handleRemove` reindexes `skuErrors` keys when splicing a new row. |
| WR-03 | `errorHandler` not defensive against non-object `err` or invalid status codes | `0b0ab07` | Confirmed — normalizes `err`, validates status is an integer in 100-599 range. |
| WR-04 | CategoryDialog `displayOrder` couldn't be cleared (NaN fails `.optional()` number schema) | `1a0a1c9` | Confirmed — `setValueAs: (v) => (v === '' ? undefined : Number(v))`. |
| WR-05 | Select components used display `name` as value/lookup key (collision risk on duplicate names) | `d298e4a` | Confirmed — both BrandDialog Manufacturer select and ListingForm Brand select now use `String(id)` as the value. |
| WR-06 | Invalid SKU quantity silently coerced to `0` via `Number(x) \|\| 0` before reaching the schema | `0560556` | Confirmed — `buildSkuCandidate` distinguishes empty (→ `undefined`) from non-numeric (passed through as-is so the schema rejects it). |

2 Info-level findings (IN-01, IN-02) were intentionally left unresolved — both are code-quality/readability notes with no behavioral impact, correctly scoped out of a blocking fix.

### Requirements Coverage

| Requirement | Source Plan | Description (from ROADMAP.md — this INSERTED phase's requirement IDs are not present in REQUIREMENTS.md, consistent with existing project convention for tech-debt/INSERTED phases, e.g. Phase 02.2's TEST-* IDs) | Status | Evidence |
|-------------|------------|------|--------|----------|
| VLDN-TIMING | 02.3-02 | Validate on blur, clear on valid change, submit catch-all + focus | ✓ SATISFIED | Truths 5-7 |
| VLDN-APIERR | 02.3-01 | Zod flatten() API error contract + typed ApiError + real message in toast | ✓ SATISFIED | Truths 1-4 |
| VLDN-SKU | 02.3-03 | SKU per-cell blur validation, tooltip, submit-time catch-all | ✓ SATISFIED | Truths 11-16 |
| VLDN-INDICATORS | 02.3-02 | Required-field asterisks, textarea character counters, required-select blur validation | ✓ SATISFIED | Truths 8-10 |

No orphaned requirements — all four IDs declared in ROADMAP.md for this phase are claimed by a plan and independently verified above.

### Human Verification Required

None outstanding. Manual UAT (the phase's designated human-verify gate, since `apps/web` has no automated test harness by established project practice) was already completed and approved by the user across all 7 flows, recorded in `02.3-04-SUMMARY.md`:
1. Categories blur/clear/submit-focus + asterisk/counter checks — approved
2. Brands blur/clear/submit-focus + counter + admin Manufacturer required — approved
3. Listings blur/clear/submit-focus + counter + Categories non-blocking — approved
4. Status select intentional non-required handling — explicitly confirmed acceptable
5. SKU cell blur → red border + tooltip; deleted rows never flagged — approved
6. SKU submit catch-all blocks save on untouched invalid row — approved
7. Server rejection shows real message in toast — approved

### Gaps Summary

None. All 16 derived observable truths are verified against current source, not SUMMARY claims. The phase went through a code review that caught 2 Critical + 6 Warning defects in the initially-shipped UI/plumbing layer (notably: SKU submit-catch-all silently failing with no visible error for 5 of 9 fields, and a "required" Manufacturer field that the schema never actually enforced) — all 8 were fixed in dedicated commits, and I independently re-read the current source (not the review's or SUMMARY's claims) to confirm each fix is genuinely present and correctly wired end-to-end (including the server-side backstop for the Manufacturer-required rule). Automated gates (API test suite 138/138, web tsc clean, full monorepo build clean) were independently re-run and passed. No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) found in any of the 11 phase-modified files.

---

_Verified: 2026-07-01T02:39:23Z_
_Verifier: Claude (gsd-verifier)_
