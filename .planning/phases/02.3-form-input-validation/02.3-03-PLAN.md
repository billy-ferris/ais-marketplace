---
phase: 02.3-form-input-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/manage/SkuInlineEditor.tsx
autonomous: true
requirements: [VLDN-SKU]

must_haves:
  truths:
    - "Blurring an invalid SKU cell (empty name, non-decimal price/MSRP, invalid quantity) marks it invalid with a red border"
    - "Hovering an invalid SKU cell shows a tooltip with the validation message"
    - "A cell's error clears once its value becomes valid"
    - "Rows marked for deletion (_deleted) are never flagged as invalid"
    - "Price and MSRP are validated on blur only (no keystroke restriction)"
  artifacts:
    - path: "apps/web/src/components/manage/SkuInlineEditor.tsx"
      provides: "Per-cell blur validation via createSkuSchema.safeParse + Base UI Tooltip on invalid cells"
      contains: "safeParse"
  key_links:
    - from: "apps/web/src/components/manage/SkuInlineEditor.tsx"
      to: "packages/shared/src/schemas/inventory-sku.ts"
      via: "createSkuSchema.safeParse per row on blur"
      pattern: "createSkuSchema"
    - from: "apps/web/src/components/manage/SkuInlineEditor.tsx"
      to: "apps/web/src/components/ui/tooltip.tsx"
      via: "Tooltip/TooltipTrigger(render)/TooltipContent + mounted TooltipProvider"
      pattern: "TooltipProvider"
---

<objective>
Retrofit the non-RHF SkuInlineEditor with a local, blur-triggered validation layer that reuses the shared createSkuSchema — red border + Base UI Tooltip on invalid cells — WITHOUT converting the component to useFieldArray (RESEARCH anti-pattern; it has _deleted/undo/image-upload semantics that must be preserved).

Purpose: SkuInlineEditor currently has zero validation and is the largest delta in the phase. Its per-cell validation must match the main-form timing (validate on blur, clear on valid).
Output: SkuInlineEditor with local skuErrors state, blur handlers on the required cells, and tooltip-wrapped invalid inputs under a mounted TooltipProvider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-form-input-validation/02.3-CONTEXT.md
@.planning/phases/02.3-form-input-validation/02.3-RESEARCH.md

<interfaces>
<!-- Shared schema (packages/shared/src/schemas/inventory-sku.ts) — all values are strings in the form,
     coerced at submit. Required: name (min 1), price (decimal regex), msrp (decimal regex),
     quantity (int nonnegative). Optional: sku, upc, size, casePack, casesPerPallet, imageUrl. -->
createSkuSchema: name, price /^\d+(\.\d{1,2})?$/, msrp same, quantity int>=0, ...

<!-- SkuFormData (current file): name, sku, upc, size, casePack, casesPerPallet, price, msrp,
     quantity, imageUrl (all string|null), optional id, optional _deleted -->

<!-- Base UI Tooltip (apps/web/src/components/ui/tooltip.tsx). TooltipTrigger uses a `render` prop.
     TooltipProvider is exported but NOT mounted anywhere in src (RESEARCH Pitfall 5). -->
Tooltip (Root), TooltipTrigger({ render }), TooltipContent, TooltipProvider

<!-- The Input component already applies red border via aria-invalid:border-destructive classes,
     so setting aria-invalid={true} yields the red border; an explicit border class may be redundant. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local per-cell validation state and blur handler</name>
  <files>apps/web/src/components/manage/SkuInlineEditor.tsx</files>
  <read_first>
    - apps/web/src/components/manage/SkuInlineEditor.tsx (full component: SkuFormData, handleFieldChange, the required inputs name/price/msrp/quantity, _deleted row rendering, buildSkuPayload)
    - packages/shared/src/schemas/inventory-sku.ts (createSkuSchema — exact rules and messages)
    - apps/web/src/components/ui/input.tsx (confirm aria-invalid:border-destructive styling exists)
  </read_first>
  <action>
    Add `const [skuErrors, setSkuErrors] = useState<Record<string, string>>({})` keyed by `${index}:${field}`. Add a `validateSkuField(index, field, row)` function: build a candidate object matching createSkuSchema's expected types from the row's string values (name: row.name; price: row.price; msrp: row.msrp; quantity: Number(row.quantity) || 0; casePack/casesPerPallet: value ? Number(value) : undefined; pass through optional string fields), run `createSkuSchema.safeParse(candidate)`, then set `skuErrors[`${index}:${field}`]` to the message of the first issue whose `path[0] === field` (or '' when none). Wire this to the required cells only — name, price, msrp, quantity — via each Input's `onBlur={() => validateSkuField(index, field, sku)}`. Do NOT add onChange numeric filtering to price/MSRP (blur-only per CONTEXT). To satisfy clear-on-valid, also re-run validateSkuField for a field inside its existing onChange ONLY when that field currently has an error (mirrors reValidateMode:'onChange'). Skip validation entirely for `_deleted` rows (they render in the separate deleted branch and are excluded from buildSkuPayload). Add an `validateAllRows()` helper (exported or exposed via ref/callback as needed by ListingForm's submit is out of scope here — keep it internal) that validates every non-deleted row; wire it only if trivially callable, otherwise leave per-blur + on-change behavior as the validation surface for this plan.
  </action>
  <verify>
    <automated>pnpm --filter @ais/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: SkuInlineEditor.tsx declares `skuErrors` state keyed by `index:field`.
    - Source: a validation function calls `createSkuSchema.safeParse` and reads `path[0] === field` from issues.
    - Source: name, price, msrp, quantity inputs have onBlur calling the validator; price/msrp have NO onChange numeric filtering.
    - Source: `_deleted` rows are not validated.
    - Behavior: `pnpm --filter @ais/web exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Blur on a required SKU cell computes a per-cell error using the shared schema; errors clear on valid change; deleted rows are skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Render red border + Base UI Tooltip on invalid cells under a mounted provider</name>
  <files>apps/web/src/components/manage/SkuInlineEditor.tsx</files>
  <read_first>
    - apps/web/src/components/manage/SkuInlineEditor.tsx (the four required Input cells to wrap; the outer container return)
    - apps/web/src/components/ui/tooltip.tsx (Tooltip, TooltipTrigger render prop, TooltipContent, TooltipProvider)
  </read_first>
  <action>
    Import Tooltip, TooltipTrigger, TooltipContent, TooltipProvider from '@/components/ui/tooltip'. Mount `<TooltipProvider>` once at the root of the component's returned tree (wrap the existing outermost `<div className="space-y-4">`) so all SKU tooltips share behavior (RESEARCH Pitfall 5). For each required cell (name, price, msrp, quantity), when `skuErrors[`${index}:${field}`]` is truthy, wrap the Input in `<Tooltip><TooltipTrigger render={<Input ... aria-invalid onBlur=... />} /><TooltipContent>{message}</TooltipContent></Tooltip>`; set `aria-invalid={!!skuErrors[key]}` on the Input so the existing aria-invalid:border-destructive styling produces the red border (add an explicit `border-destructive` class only if visual check shows the aria styling is insufficient). Keep the value/onChange/disabled props exactly as they are today. Non-required cells (sku, upc, size, casePack, casesPerPallet, image) are unchanged. Preserve the _deleted-row and Add-SKU rendering untouched.
  </action>
  <verify>
    <automated>pnpm --filter @ais/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: SkuInlineEditor imports TooltipProvider and mounts it around the component tree.
    - Source: the four required inputs are wrapped in Tooltip/TooltipTrigger(render)/TooltipContent when their cell has an error, with `aria-invalid` reflecting the error.
    - Source: optional cells and the _deleted/Add-SKU rendering are unchanged.
    - Behavior (manual): entering a non-decimal price and blurring shows a red border and a hover tooltip with "Must be a decimal number..."; clearing name and blurring shows "SKU name is required"; fixing the value removes border and tooltip.
    - Behavior: `pnpm --filter @ais/web exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Invalid SKU cells show a red border and a hover tooltip carrying the shared-schema message; the tooltip provider is mounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → SKU editor | Client-side per-cell validation is UX only; server createSkuSchema.parse() remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-04 | Tampering | SkuInlineEditor client validation | accept | Local safeParse is UX only; server-side SKU validation is unchanged and authoritative. |
| T-02.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs; Tooltip and zod schema already present. |
</threat_model>

<verification>
- `pnpm --filter @ais/web exec tsc --noEmit` clean.
- Manual UAT: invalid price/msrp/name/quantity on blur → red border + tooltip; valid value clears; deleted rows never flagged.
</verification>

<success_criteria>
- Required SKU cells validate on blur against createSkuSchema with red border + tooltip.
- Errors clear on valid change; deleted rows excluded.
- No keystroke restriction on price/MSRP.
- TooltipProvider mounted.
</success_criteria>

<output>
Create `.planning/phases/02.3-form-input-validation/02.3-03-SUMMARY.md` when done
</output>
