---
context: phase
phase: 02.3-form-input-validation
task: null
total_tasks: null
status: not_started
last_updated: 2026-07-01T00:21:27.018Z
---

_No blocking constraints identified this session._

<current_state>
Phase 02.3 (Form Input Validation) has context gathered but no PLAN.md yet. This session was orientation-only:
- Reviewed 02.3-CONTEXT.md decisions
- Audited current form validation state across the codebase
- Updated GSD from v1.41.0 to v1.42.3
- No code written

Next step is planning.
</current_state>

<completed_work>
Nothing implemented yet. Phase 02.2 (populate test stubs) is fully verified and complete (98/98 tests passing, 0 it.todo() stubs remaining).
</completed_work>

<remaining_work>
- Run /gsd-plan-phase 02.3 to break down 02.3-CONTEXT.md into PLAN.md tasks
- Execute plans:
  - Add `mode: 'onBlur'` + positive feedback (clear on change once valid) to ListingForm, BrandDialog, CategoryDialog
  - Add react-hook-form + zodResolver + error display to SkuInlineEditor (currently has NO validation)
  - Add required field asterisks to all forms
  - Add character counters on textarea fields (description fields)
  - Add `aria-invalid` to Select fields (missing on brandId in ListingForm, companyId in BrandDialog)
  - Restructure API error format: `{ error, fieldErrors, formErrors }` (currently returns `{ error, details: string[] }`)
  - Create `ApiError` class extending Error with `fieldErrors`, `formErrors`, `status` properties
  - Update `apiFetch` in `apps/web/src/lib/api.ts` to parse new structured error format
  - Update mutation hooks to use ApiError for error handling
</remaining_work>

<decisions_made>
All decisions are in 02.3-CONTEXT.md. Key ones:
- Validation timing: blur → clear on change once valid → all on submit (shouldFocusError)
- Server errors: toast-only display; restructure format NOW for future field mapping
- SKU row errors: red border + tooltip on hover (compact for table rows)
- Required indicators: red asterisk on required labels; character counter on textareas only; no "(optional)" labels
- ApiError class must be instanceof-checkable
- shadcn Form components vs raw register/errors: Claude's discretion
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02.3-form-input-validation/02.3-CONTEXT.md` — all implementation decisions are here; read before planning
2. `apps/web/src/components/manage/ListingForm.tsx` — uses react-hook-form but only onSubmit mode; missing aria-invalid on brandId Select
3. `apps/web/src/components/manage/BrandDialog.tsx` — same; missing aria-invalid on companyId Select
4. `apps/web/src/components/manage/CategoryDialog.tsx` — same; fullest aria-invalid coverage of the three
5. `apps/web/src/components/manage/SkuInlineEditor.tsx` — NO react-hook-form, NO Zod, NO error display; biggest gap
6. `apps/api/src/middleware/error.ts` — current ZodError handler returns `{ error, details: string[] }`; needs restructure to `{ error, fieldErrors, formErrors }`
7. `apps/web/src/lib/api.ts` — apiFetch utility; needs ApiError class and structured error parsing

## Critical Anti-Patterns (do NOT repeat these)
None discovered this session.

## Infrastructure State
- Dev server: not running
- DB: PostgreSQL (connection details in root `.env`)
- NODE_TLS_REJECT_UNAUTHORIZED=0 required in root `.env` for Clerk JWKS in dev

<context>
Orientation session only. The codebase audit revealed:
- 3 dialog forms (ListingForm, BrandDialog, CategoryDialog): have react-hook-form + zodResolver but default onSubmit mode only; render error messages; mostly have aria-invalid but missing on Select fields
- SkuInlineEditor: completely unvalidated — local state management, no Zod, no error display
- API error middleware: returns `{ error: 'Validation failed', details: ['field: message'] }` — not yet structured for field mapping

GSD was updated to v1.42.3 this session (from v1.41.0). Restart Claude Code to pick up new commands.
</context>

<next_action>
Start with: `/gsd-plan-phase 02.3` — the CONTEXT.md has all decisions; planning should be straightforward.
</next_action>
