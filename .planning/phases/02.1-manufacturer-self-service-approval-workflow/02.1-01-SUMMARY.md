---
phase: 02.1-manufacturer-self-service-approval-workflow
plan: 01
subsystem: database, api, testing
tags: [drizzle, pgEnum, notifications, approval-workflow, zod]

# Dependency graph
requires:
  - phase: 02-catalog-management
    provides: brand_listings table, listing_status enum, shared ListingStatus types
provides:
  - Extended listing_status enum with pending_approval and rejected
  - brand_listings.rejection_reason column
  - notifications table with notification_type enum
  - notificationsRelations and updated usersRelations
  - Shared ListingStatus with PENDING_APPROVAL and REJECTED
  - API_ROUTES.NOTIFICATIONS constant
  - Zod listing schemas accepting new status values
  - Wave 0 test stubs for Phase 2.1 (32 it.todo)
affects: [02.1-02, 02.1-03, approval-workflow, notifications, manufacturer-scoping]

# Tech tracking
tech-stack:
  added: []
  patterns: [pgEnum value extension by append, notification table pattern]

key-files:
  created:
    - apps/api/src/db/schema/notifications.ts
    - apps/api/src/__tests__/routes/notifications.test.ts
    - apps/api/src/__tests__/services/email.test.ts
  modified:
    - apps/api/src/db/schema/brand-listings.ts
    - apps/api/src/db/schema/relations.ts
    - apps/api/src/db/schema/index.ts
    - packages/shared/src/types/catalog.ts
    - packages/shared/src/constants/routes.ts
    - packages/shared/src/schemas/listing.ts
    - apps/api/src/__tests__/routes/listings.test.ts
    - apps/api/src/__tests__/routes/brands.test.ts
    - apps/api/src/__tests__/schemas/catalog.test.ts

key-decisions:
  - "Appended pending_approval and rejected to existing pgEnum (Postgres only supports append, not reorder)"
  - "Notification table uses integer FK to users.id (not clerk_id) for DB-level referential integrity"

patterns-established:
  - "pgEnum extension: always append new values at end of array"
  - "Notifications table pattern: userId FK, type enum, entityType/entityId polymorphic reference"

requirements-completed: [APRV-01, APRV-06, APRV-07]

# Metrics
duration: 3min
completed: 2026-03-09
---

# Phase 2.1 Plan 01: Schema Foundation & Shared Types Summary

**Extended listing_status enum with approval states, created notifications table, and added 32 Wave 0 test stubs for manufacturer self-service approval workflow**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-10T00:03:47Z
- **Completed:** 2026-03-10T00:07:29Z
- **Tasks:** 2
- **Files modified:** 12

## Accomplishments
- Extended listing_status pgEnum with pending_approval and rejected values, plus rejection_reason column on brand_listings
- Created notifications table with notification_type enum (listing_submitted, listing_approved, listing_rejected) and user FK
- Updated shared types (ListingStatus, BrandListing, API_ROUTES, Zod schemas) for downstream consumption
- Created 32 it.todo test stubs across 5 files covering notifications, email service, manufacturer scoping, and approval workflow

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 test stubs for Phase 2.1** - `9d3f1c4` (test)
2. **Task 2: Extend DB schema and shared types** - `5db4209` (feat)

## Files Created/Modified
- `apps/api/src/db/schema/notifications.ts` - New notifications table with pgEnum for notification types
- `apps/api/src/db/schema/brand-listings.ts` - Extended listing_status enum + rejectionReason column
- `apps/api/src/db/schema/relations.ts` - Added notificationsRelations, updated usersRelations with notifications
- `apps/api/src/db/schema/index.ts` - Barrel export for notifications table and relations
- `packages/shared/src/types/catalog.ts` - Extended ListingStatus const, labels, and BrandListing interface
- `packages/shared/src/constants/routes.ts` - Added NOTIFICATIONS route constant
- `packages/shared/src/schemas/listing.ts` - Extended Zod status enum with pending_approval and rejected
- `apps/api/src/__tests__/routes/notifications.test.ts` - 8 it.todo stubs for notification routes
- `apps/api/src/__tests__/services/email.test.ts` - 5 it.todo stubs for email service
- `apps/api/src/__tests__/routes/listings.test.ts` - Extended with 12 it.todo stubs (manufacturer scoping + approval workflow)
- `apps/api/src/__tests__/routes/brands.test.ts` - Extended with 4 it.todo stubs (manufacturer scoping)
- `apps/api/src/__tests__/schemas/catalog.test.ts` - Extended with 3 it.todo stubs (ListingStatus enum)

## Decisions Made
- Appended pending_approval and rejected to existing pgEnum array (Postgres only supports appending, not reordering enum values)
- Notification table uses integer FK to users.id (not clerk_id) for DB-level referential integrity

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Schema foundation complete for Plans 02 (approval routes) and 03 (notification routes, email service)
- All shared types and constants available for downstream route implementations
- 32 test stubs ready to be populated as features are implemented

---
*Phase: 02.1-manufacturer-self-service-approval-workflow*
*Completed: 2026-03-09*
