---
phase: 02.2-populate-phase-2-test-stubs-with-real-implementations
plan: 02
subsystem: testing
tags: [vitest, express, drizzle, mocking, route-handlers, brands, categories]

# Dependency graph
requires:
  - phase: 02-admin-catalog-management
    provides: "Brand and category route handlers with CRUD operations"
provides:
  - "17 passing brand route handler tests covering all CRUD operations"
  - "13 passing category route handler tests covering all CRUD operations"
  - "Reusable mock db pattern with vi.hoisted() for Drizzle chainable queries"
affects: [02.2-03, testing]

# Tech tracking
tech-stack:
  added: []
  patterns: [vi.hoisted for mock db, thenable mockDb for Promise.all, handler extraction from Express router stack]

key-files:
  created: []
  modified:
    - apps/api/src/__tests__/routes/brands.test.ts
    - apps/api/src/__tests__/routes/categories.test.ts

key-decisions:
  - "vi.hoisted() pattern for mockDb to avoid hoisting reference errors in vi.mock factories"
  - "Thenable mockDb with call counter for Promise.all resolution in list routes"
  - "Route middleware stack inspection for admin role verification (clearAllMocks clears registration-time calls)"

patterns-established:
  - "vi.hoisted mockDb: Use vi.hoisted(() => { const mockDb: any = {}; return { mockDb }; }) to create mock db accessible in vi.mock factories"
  - "Thenable mock for Promise.all: Set mockDb.then as vi.fn with call counter to return different data per Promise.all entry"
  - "Handler extraction: getHandler(router, method, path) extracts final handler from Express router stack for direct invocation"

requirements-completed: [TEST-BRANDS, TEST-CATEGORIES]

# Metrics
duration: 4min
completed: 2026-03-09
---

# Phase 02.2 Plan 02: Brand & Category Route Handler Tests Summary

**30 route handler tests (17 brands + 13 categories) using vi.hoisted mockDb pattern with thenable Drizzle chain mocking**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-09T23:50:29Z
- **Completed:** 2026-03-09T23:54:49Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced all 17 it.todo() stubs in brands.test.ts with passing test implementations
- Replaced all 13 it.todo() stubs in categories.test.ts with passing test implementations
- Zero it.todo() stubs remaining in either file
- Full test suite (98 tests across 8 files) passes with no regressions
- Established reusable vi.hoisted mockDb pattern for Drizzle ORM query chain mocking

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement 17 brand route handler tests** - `4ca8312` (test)
2. **Task 2: Implement 13 category route handler tests** - `56022f0` (test)

**Plan metadata:** (pending) (docs: complete plan)

## Files Created/Modified
- `apps/api/src/__tests__/routes/brands.test.ts` - 17 brand route handler tests: list with pagination/filters, get by ID, create with slug gen, update with slug regen, soft-delete
- `apps/api/src/__tests__/routes/categories.test.ts` - 13 category route handler tests: list with pagination/search/ordering, get by ID, create with slug gen, update, soft-delete

## Decisions Made
- Used `vi.hoisted()` to declare mockDb so it is accessible within hoisted `vi.mock()` factory functions -- avoids "Cannot access before initialization" errors
- Used thenable mockDb pattern (custom `.then` method with call counter) to support `Promise.all([dataQuery, countQuery])` in list routes where both queries share the same mock chain
- Changed "should require admin role" test from checking `requireRole` mock call count to inspecting router middleware stack length, since `vi.clearAllMocks()` in `beforeEach` clears call history from import-time route registration

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed "should require admin role" test approach**
- **Found during:** Task 1 (brands.test.ts)
- **Issue:** Plan suggested checking `expect(requireRole).toHaveBeenCalledWith('admin')` but `vi.clearAllMocks()` in beforeEach clears the mock call history from route registration time, so the assertion always fails with 0 calls
- **Fix:** Changed to inspect the router's middleware stack length directly, verifying that POST / has multiple middleware handlers (auth + role + handler)
- **Files modified:** apps/api/src/__tests__/routes/brands.test.ts
- **Verification:** All 17 tests pass
- **Committed in:** 4ca8312 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minor test assertion adjustment. No scope change.

## Issues Encountered
- `vi.mock` factory hoisting caused "Cannot access 'mockDb' before initialization" -- resolved by using `vi.hoisted()` to declare mockDb in the hoisted scope

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Brand and category route tests complete, establishing mock patterns for the listings route tests in plan 02.2-03
- The vi.hoisted mockDb pattern and thenable chain approach are directly reusable for the more complex listings route tests

## Self-Check: PASSED

- [x] brands.test.ts exists
- [x] categories.test.ts exists
- [x] Commit 4ca8312 exists (Task 1)
- [x] Commit 56022f0 exists (Task 2)

---
*Phase: 02.2-populate-phase-2-test-stubs-with-real-implementations*
*Completed: 2026-03-09*
