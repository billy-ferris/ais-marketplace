---
phase: 02-admin-catalog-management
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - apps/api/src/routes/brands.ts
  - apps/api/src/routes/categories.ts
  - apps/api/src/routes/uploads.ts
  - apps/api/src/index.ts
  - apps/api/.env.example
  - apps/web/src/components/shared/DataTable.tsx
  - apps/web/src/components/shared/ConfirmDeleteDialog.tsx
  - apps/web/src/components/shared/ImageUploader.tsx
  - apps/web/src/components/shared/ImagePlaceholder.tsx
autonomous: true
requirements:
  - ADMN-02

must_haves:
  truths:
    - "Brand CRUD API responds correctly (GET list, GET by ID, POST, PATCH, DELETE soft)"
    - "Category CRUD API responds correctly (GET list, GET by ID, POST, PATCH, DELETE soft)"
    - "Upload API returns presigned URL for R2 image upload"
    - "DataTable component renders sortable, paginated table data"
    - "ConfirmDeleteDialog shows confirmation before soft-delete"
  artifacts:
    - path: "apps/api/src/routes/brands.ts"
      provides: "Brand CRUD API (GET list, GET by ID, POST, PATCH, DELETE soft)"
      exports: ["brandRouter"]
    - path: "apps/api/src/routes/categories.ts"
      provides: "Category CRUD API (GET list, GET by ID, POST, PATCH, DELETE soft)"
      exports: ["categoryRouter"]
    - path: "apps/api/src/routes/uploads.ts"
      provides: "Presigned URL generation endpoint"
      exports: ["uploadRouter"]
    - path: "apps/web/src/components/shared/DataTable.tsx"
      provides: "Reusable TanStack Table wrapper component"
      exports: ["DataTable"]
    - path: "apps/web/src/components/shared/ConfirmDeleteDialog.tsx"
      provides: "Soft delete confirmation dialog"
      exports: ["ConfirmDeleteDialog"]
    - path: "apps/web/src/components/shared/ImageUploader.tsx"
      provides: "Image upload component with preview"
      exports: ["ImageUploader"]
  key_links:
    - from: "apps/api/src/routes/brands.ts"
      to: "apps/api/src/db/schema/brands.ts"
      via: "Drizzle db.select/insert/update on brands table"
      pattern: "db\\.(select|insert|update).*brands"
    - from: "apps/api/src/routes/categories.ts"
      to: "apps/api/src/db/schema/categories.ts"
      via: "Drizzle db.select/insert/update on categories table"
      pattern: "db\\.(select|insert|update).*categories"
    - from: "apps/api/src/routes/uploads.ts"
      to: "@aws-sdk/client-s3"
      via: "S3Client + PutObjectCommand for R2"
      pattern: "S3Client|PutObjectCommand"

user_setup:
  - service: cloudflare-r2
    why: "Image storage for brand logos, listing images, SKU images"
    env_vars:
      - name: R2_ACCOUNT_ID
        source: "Cloudflare Dashboard -> R2 -> Overview -> Account ID (top right)"
      - name: R2_ACCESS_KEY_ID
        source: "Cloudflare Dashboard -> R2 -> Overview -> Manage R2 API Tokens -> Create API Token -> Access Key ID"
      - name: R2_SECRET_ACCESS_KEY
        source: "Same token creation page -> Secret Access Key (shown once)"
      - name: R2_BUCKET_NAME
        source: "Cloudflare Dashboard -> R2 -> Create Bucket -> bucket name you chose"
      - name: R2_PUBLIC_URL
        source: "Cloudflare Dashboard -> R2 -> Bucket -> Settings -> Public Access -> r2.dev subdomain URL or custom domain"
    dashboard_config:
      - task: "Create R2 bucket"
        location: "Cloudflare Dashboard -> R2 -> Create Bucket"
      - task: "Enable public access (r2.dev subdomain for dev)"
        location: "Cloudflare Dashboard -> R2 -> Bucket -> Settings -> Public Access"
      - task: "Configure CORS: Allow PUT from http://localhost:5173, allow Content-Type header, expose ETag"
        location: "Cloudflare Dashboard -> R2 -> Bucket -> Settings -> CORS Policy"
---

<objective>
Build brand, category, and upload API routes plus reusable shared UI components (DataTable, dialogs, image upload).

Purpose: This plan delivers the backend API layer for brand and category CRUD, the presigned URL upload endpoint for R2, and the foundational shared UI components that both brand/category pages and listing pages will reuse.

Output: 3 registered API routes (brands, categories, uploads) + 4 shared UI components (DataTable, ConfirmDeleteDialog, ImageUploader, ImagePlaceholder).
</objective>

<execution_context>
@/Users/billy.ferris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billy.ferris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-catalog-management/02-CONTEXT.md
@.planning/phases/02-admin-catalog-management/02-RESEARCH.md
@.planning/phases/02-admin-catalog-management/02-01-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-02-SUMMARY.md

<!-- Both prior SUMMARY files needed: Plan 01 for schema tables, Plan 02 for shared types/schemas/constants and router setup -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand, category, and upload API routes</name>
  <files>
    apps/api/src/routes/brands.ts,
    apps/api/src/routes/categories.ts,
    apps/api/src/routes/uploads.ts,
    apps/api/src/index.ts,
    apps/api/.env.example
  </files>
  <action>
**Brand API route (apps/api/src/routes/brands.ts):** Follow the exact pattern from companies.ts with `const router: RouterType = Router()` and try/catch with next(err).

- GET / -- List brands. requireAuth() + requireRole('admin'). Filter: isNull(brands.deletedAt). Support query params: search (ilike on brands.name), companyId (eq filter), page (default 1), limit (default 20). Return array of brands with pagination metadata (total count via separate count query). Join with companies to include company name in response.
- GET /:id -- Get brand by ID. requireAuth() + requireRole('admin'). Filter: isNull(brands.deletedAt), eq(brands.id, id). Include company relation. Return 404 if not found.
- POST / -- Create brand. requireAuth() + requireRole('admin'). Parse body with createBrandSchema from @ais/shared/schemas. Auto-generate slug from name (lowercase, replace spaces/special chars with hyphens, trim). If slug exists, append -2, -3, etc. Insert and return 201.
- PATCH /:id -- Update brand. requireAuth() + requireRole('admin'). Parse body with updateBrandSchema. If name provided, regenerate slug (with uniqueness check). Set updatedAt to new Date(). Return 404 if not found or deleted.
- DELETE /:id -- Soft delete brand. requireAuth() + requireRole('admin'). Set deletedAt to new Date(). Return 404 if not found or already deleted.

**Category API route (apps/api/src/routes/categories.ts):** Same pattern.

- GET / -- List categories. requireAuth() + requireRole('admin'). Filter: isNull(categories.deletedAt). Support: search (ilike name), page, limit. Order by displayOrder ASC. Return with total count.
- GET /:id -- Get category by ID. Filter: not deleted. Return 404 if not found.
- POST / -- Create category. Parse with createCategorySchema. Auto-generate slug from name. Insert and return 201.
- PATCH /:id -- Update. If name provided, regenerate slug. Set updatedAt.
- DELETE /:id -- Soft delete with deletedAt.

**Upload API route (apps/api/src/routes/uploads.ts):**

- Create S3Client configured for R2: region 'auto', endpoint from R2_ACCOUNT_ID env var, credentials from R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY env vars.
- POST /presigned-url -- requireAuth() + requireRole('admin'). Accept {fileName, contentType} in body. Generate unique key: `images/${Date.now()}-${sanitizedFileName}`. Create PutObjectCommand with Bucket (R2_BUCKET_NAME), Key, ContentType. Use getSignedUrl with 600s (10 min) expiry. Return { uploadUrl, publicUrl: `${R2_PUBLIC_URL}/${key}`, key }.
- PITFALL: ContentType in PutObjectCommand MUST match the Content-Type header the browser sends. Always use the contentType from the request body.

**Register routes in apps/api/src/index.ts:** Add `app.use(API_ROUTES.BRANDS, brandRouter)`, `app.use(API_ROUTES.CATEGORIES, categoryRouter)`, `app.use(API_ROUTES.UPLOADS, uploadRouter)` after existing route mounts. Import from new route files.

**Update apps/api/.env.example:** Add R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_PUBLIC_URL entries.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm turbo run build --filter=api 2>&1 | tail -5 && cd apps/api && npx vitest run src/__tests__/routes/brands.test.ts src/__tests__/routes/categories.test.ts src/__tests__/routes/uploads.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Brand, category, and upload API routes compile and are registered in Express. API build passes. Test stubs run (report todos).</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable shared UI components (DataTable, ConfirmDeleteDialog, ImageUploader, ImagePlaceholder)</name>
  <files>
    apps/web/src/components/shared/DataTable.tsx,
    apps/web/src/components/shared/ConfirmDeleteDialog.tsx,
    apps/web/src/components/shared/ImageUploader.tsx,
    apps/web/src/components/shared/ImagePlaceholder.tsx
  </files>
  <action>
**DataTable.tsx (apps/web/src/components/shared/DataTable.tsx):** Create a generic, reusable data table component wrapping TanStack Table v8 + shadcn/ui Table:
- Generic props: `columns: ColumnDef<TData, TValue>[]`, `data: TData[]`, `isLoading: boolean`, `pageCount?: number`, `pagination?: PaginationState`, `onPaginationChange?: OnChangeFn<PaginationState>`, `sorting?: SortingState`, `onSortingChange?: OnChangeFn<SortingState>`.
- Use getCoreRowModel, getSortedRowModel, getPaginationRowModel from TanStack Table.
- Render shadcn Table with TableHeader, TableBody, TableRow, TableHead, TableCell.
- Sortable column headers: clicking toggles asc/desc with visual indicator (ChevronUp/ChevronDown icons).
- Pagination controls at bottom: Previous/Next buttons, "Page X of Y" display.
- Loading state: show Skeleton rows when isLoading is true.
- Empty state: show "No results found" when data is empty and not loading.

**ConfirmDeleteDialog.tsx:** shadcn AlertDialog for soft-delete confirmation. Props: open, onOpenChange, title (default "Are you sure?"), description, onConfirm, isPending (for loading state on confirm button). Confirm button is destructive variant.

**ImageUploader.tsx:** Reusable image upload component. Props: value (current imageUrl or null), onChange (callback with new URL), onRemove (callback to clear), accept (default "image/*"), maxSizeMB (default 5), disabled. Shows current image preview if value exists, or a dashed-border dropzone with Upload icon and "Click or drag to upload" text. On file select, validates size, then calls onChange with the File object. The parent component handles the actual upload via useUpload hook. Show loading spinner during upload.

**ImagePlaceholder.tsx:** Simple placeholder component for when no image exists. Props: className, iconSize. Renders a gray background with ImageIcon from lucide-react centered.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm turbo run build 2>&1 | tail -10</automated>
  </verify>
  <done>DataTable, ConfirmDeleteDialog, ImageUploader, and ImagePlaceholder components compile. Full monorepo build passes.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run build` passes without errors
- Brand CRUD: POST /api/brands creates brand, GET /api/brands lists (excluding soft-deleted), PATCH updates, DELETE soft-deletes
- Category CRUD: same operations work for categories
- Upload: POST /api/uploads/presigned-url returns valid presigned URL and public URL
- Shared UI components compile and export correctly
- `cd apps/api && pnpm test` passes (stubs report todos)
</verification>

<success_criteria>
- Brand API route with full CRUD is registered at /api/brands
- Category API route with full CRUD is registered at /api/categories
- Upload API route for presigned URLs is registered at /api/uploads
- DataTable component is reusable with generic column definitions
- ConfirmDeleteDialog provides soft-delete confirmation UX
- ImageUploader handles file selection with preview
- Full monorepo build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-catalog-management/02-03-SUMMARY.md`
</output>
