---
phase: 02-admin-catalog-management
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - apps/web/src/hooks/useBrands.ts
  - apps/web/src/hooks/useCategories.ts
  - apps/web/src/hooks/useUpload.ts
  - apps/web/src/components/manage/BrandDialog.tsx
  - apps/web/src/components/manage/CategoryDialog.tsx
  - apps/web/src/components/manage/columns/brand-columns.tsx
  - apps/web/src/components/manage/columns/category-columns.tsx
  - apps/web/src/pages/manage/BrandsPage.tsx
  - apps/web/src/pages/manage/CategoriesPage.tsx
autonomous: true
requirements:
  - ADMN-02

must_haves:
  truths:
    - "Admin can create a new brand with name, description, logo image, and assign it to a manufacturer company"
    - "Admin can edit an existing brand's name, description, and logo"
    - "Admin can soft-delete a brand and it disappears from the brands list"
    - "Admin can create a new category with name and Lucide icon"
    - "Admin can edit an existing category"
    - "Admin can soft-delete a category and it disappears from the categories list"
    - "Brands table is sortable and searchable by name"
    - "Categories table is sortable and searchable by name"
  artifacts:
    - path: "apps/web/src/pages/manage/BrandsPage.tsx"
      provides: "Brands management page with data table and dialog"
      exports: ["BrandsPage"]
    - path: "apps/web/src/pages/manage/CategoriesPage.tsx"
      provides: "Categories management page with data table and dialog"
      exports: ["CategoriesPage"]
    - path: "apps/web/src/hooks/useBrands.ts"
      provides: "TanStack Query hooks for brand CRUD"
      exports: ["useBrands", "useCreateBrand", "useUpdateBrand", "useDeleteBrand"]
    - path: "apps/web/src/hooks/useCategories.ts"
      provides: "TanStack Query hooks for category CRUD"
      exports: ["useCategories", "useCreateCategory", "useUpdateCategory", "useDeleteCategory"]
    - path: "apps/web/src/hooks/useUpload.ts"
      provides: "Presigned URL upload hook"
      exports: ["useUpload"]
  key_links:
    - from: "apps/web/src/hooks/useBrands.ts"
      to: "/api/brands"
      via: "TanStack Query + apiFetch"
      pattern: "useQuery.*brands"
    - from: "apps/web/src/hooks/useCategories.ts"
      to: "/api/categories"
      via: "TanStack Query + apiFetch"
      pattern: "useQuery.*categories"
    - from: "apps/web/src/hooks/useUpload.ts"
      to: "/api/uploads/presigned-url"
      via: "TanStack Query mutation + direct R2 upload"
      pattern: "presigned-url"
    - from: "apps/web/src/pages/manage/BrandsPage.tsx"
      to: "apps/web/src/components/shared/DataTable.tsx"
      via: "DataTable component renders brand data"
      pattern: "DataTable"
---

<objective>
Build BrandsPage and CategoriesPage with TanStack Query hooks, dialog modals, and column definitions.

Purpose: ADMN-02 requires admin to manage brands and categories. This plan delivers the complete frontend: TanStack Query hooks for CRUD operations, react-hook-form dialogs for create/edit, column definitions for data tables, and full management pages with search and soft-delete.

Output: Working brand and category management at /manage/brands and /manage/categories with full CRUD operations.
</objective>

<execution_context>
@/Users/billy.ferris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billy.ferris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-catalog-management/02-CONTEXT.md
@.planning/phases/02-admin-catalog-management/02-RESEARCH.md
@.planning/phases/02-admin-catalog-management/02-01-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-02-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-03-SUMMARY.md

<!-- Plan 03 SUMMARY needed: provides API routes and shared UI components this plan builds on -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TanStack Query hooks for brands, categories, and upload</name>
  <files>
    apps/web/src/hooks/useBrands.ts,
    apps/web/src/hooks/useCategories.ts,
    apps/web/src/hooks/useUpload.ts
  </files>
  <action>
**useBrands.ts:** Create hooks following the TanStack Query v5 pattern from RESEARCH.md:
- `useBrands(params?: { search?: string; page?: number; limit?: number })` -- useQuery with queryKey ['brands', 'list', params]. Calls apiFetch with query string params. Returns brand list + pagination.
- `useBrand(id: number)` -- useQuery with queryKey ['brands', 'detail', id]. Enabled only when id is truthy.
- `useCreateBrand()` -- useMutation calling POST to API_ROUTES.BRANDS. onSuccess: invalidate queryKey ['brands'] and show toast.success('Brand created').
- `useUpdateBrand()` -- useMutation calling PATCH to API_ROUTES.BRANDS/:id. onSuccess: invalidate ['brands'] and toast.success('Brand updated').
- `useDeleteBrand()` -- useMutation calling DELETE to API_ROUTES.BRANDS/:id. onSuccess: invalidate ['brands'] and toast.success('Brand deleted').
Import toast from sonner.

**useCategories.ts:** Same pattern:
- `useCategories(params?)` -- queryKey ['categories', 'list', params].
- `useCategory(id)` -- queryKey ['categories', 'detail', id].
- `useCreateCategory()`, `useUpdateCategory()`, `useDeleteCategory()` -- mutations with invalidation and toast.

**useUpload.ts:** Create upload hook:
- `useUpload()` returns `{ uploadFile, isUploading }`.
- `uploadFile(file: File)` is an async function that: (1) calls apiFetch POST to `${API_ROUTES.UPLOADS}/presigned-url` with { fileName: file.name, contentType: file.type }, (2) does a fetch PUT to the returned uploadUrl with file body and Content-Type header matching file.type, (3) returns the publicUrl string.
- Track isUploading state via internal useState.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm turbo run build --filter=web 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 hook files compile. Web build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build BrandDialog, CategoryDialog, column definitions, and management pages</name>
  <files>
    apps/web/src/components/manage/BrandDialog.tsx,
    apps/web/src/components/manage/CategoryDialog.tsx,
    apps/web/src/components/manage/columns/brand-columns.tsx,
    apps/web/src/components/manage/columns/category-columns.tsx,
    apps/web/src/pages/manage/BrandsPage.tsx,
    apps/web/src/pages/manage/CategoriesPage.tsx
  </files>
  <action>
**BrandDialog.tsx:** shadcn Dialog with react-hook-form + zodResolver:
- Props: open, onOpenChange, brand? (for edit mode, optional).
- Form fields: name (Input), description (Textarea), companyId (Select populated by fetching companies list via apiFetch -- GET /api/companies returns all companies, filter to type='manufacturer' on client), logoUrl (ImageUploader component).
- For logo: when user selects an image in ImageUploader, call uploadFile() from useUpload, then set logoUrl field value to the returned publicUrl.
- On submit: call useCreateBrand or useUpdateBrand mutation depending on whether brand prop exists.
- On success: close dialog, form resets.
- Submit button shows loading state when mutation is pending.
- Dialog title: "Create Brand" or "Edit Brand" based on mode.

**CategoryDialog.tsx:** simpler dialog:
- Props: open, onOpenChange, category? (for edit mode).
- Form fields: name (Input), icon (Input with helper text "Enter a Lucide icon name, e.g., scissors, sparkles"), displayOrder (Input type number).
- On submit: useCreateCategory or useUpdateCategory.
- On success: close dialog, form resets.

**brand-columns.tsx:** TanStack Table column definitions for brands:
- Columns: Logo (render small image or ImagePlaceholder), Name (sortable), Company (company name), Created (formatted date), Actions (DropdownMenu with Edit and Delete options).
- Actions column: Edit opens BrandDialog in edit mode, Delete opens ConfirmDeleteDialog.
- Export a function `getBrandColumns({ onEdit, onDelete })` that returns ColumnDef array. The page passes callbacks.

**category-columns.tsx:** Column definitions for categories:
- Columns: Icon (render Lucide icon by name if available, otherwise dash), Name (sortable), Display Order (sortable), Created (formatted date), Actions (Edit, Delete dropdown).
- Export `getCategoryColumns({ onEdit, onDelete })`.

**BrandsPage.tsx (replace stub):** Full management page:
- State: search string, page number, selected brand for edit, delete dialog state.
- Fetch brands with useBrands({ search, page }).
- Render: page header "Brands" with "Add Brand" button (opens BrandDialog in create mode).
- Search input below header that debounces and sets search state.
- DataTable with brand-columns, brands data, loading state.
- BrandDialog (controlled by open state).
- ConfirmDeleteDialog (controlled by delete state, onConfirm calls useDeleteBrand).

**CategoriesPage.tsx (replace stub):** Same pattern:
- useCategoriesPage with search, page state.
- DataTable with category-columns.
- CategoryDialog for create/edit.
- ConfirmDeleteDialog for delete.
- "Add Category" button in header.

IMPORTANT: All API calls go through apiFetch() which includes credentials. All mutations use TanStack Query's invalidation to refresh the list after changes. Use sonner toast for success/error feedback.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace && pnpm turbo run build 2>&1 | tail -10 && cd apps/api && npx vitest run src/__tests__/routes/brands.test.ts src/__tests__/routes/categories.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>BrandsPage renders data table with brand data from API, supports search, create via dialog, edit via dialog, soft-delete via confirmation dialog with toast feedback. CategoriesPage renders same pattern for categories. Brand logo upload works via presigned URL pipeline. Full monorepo build passes. Test stubs run.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run build` passes without errors
- BrandsPage: table renders brands, search filters by name, Add Brand dialog creates, Edit updates, Delete soft-deletes with confirmation
- CategoriesPage: same functionality for categories
- Toast notifications appear for all CRUD operations
- Soft-deleted records do not appear in list views
- `cd apps/api && pnpm test` passes (stubs report todos)
</verification>

<success_criteria>
- Admin can navigate to /manage/brands, see all brands in a sortable/searchable table
- Admin can create a brand with name, description, logo, and company assignment via dialog
- Admin can edit a brand via dialog
- Admin can soft-delete a brand with confirmation dialog
- Admin can navigate to /manage/categories, see all categories in a sortable/searchable table
- Admin can create, edit, and soft-delete categories via dialog
- Presigned URL upload endpoint generates valid R2 upload URLs
- All CRUD operations show toast feedback
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-catalog-management/02-04-SUMMARY.md`
</output>
