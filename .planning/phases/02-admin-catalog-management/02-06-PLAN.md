---
phase: 02-admin-catalog-management
plan: 06
type: execute
wave: 5
depends_on:
  - 02-04
  - 02-05
files_modified:
  - apps/api/src/db/seed.ts
autonomous: false
requirements:
  - ADMN-01
  - ADMN-02

must_haves:
  truths:
    - "Seed script populates brands, categories, listings, and SKUs in the database for demo purposes"
    - "Admin can see pre-populated data in all management tables after seeding"
    - "Admin can perform full CRUD on brands, categories, and listings through the UI"
    - "Changes persist across page refresh"
    - "Soft-deleted items do not appear in any list view"
  artifacts:
    - path: "apps/api/src/db/seed.ts"
      provides: "Extended seed script with catalog demo data"
      contains: "brands"
  key_links:
    - from: "apps/api/src/db/seed.ts"
      to: "apps/api/src/db/schema/index.ts"
      via: "Drizzle inserts into brands, categories, brand_listings, inventory_skus, listing_categories tables"
      pattern: "db\\.insert.*brands|categories|brandListings|inventorySkus"
---

<objective>
Extend the seed script with catalog demo data (brands, categories, listings, SKUs) and verify the complete admin catalog management experience end-to-end.

Purpose: Without seed data, the admin experience is empty tables. Seeding provides a realistic demo environment and enables full verification that ADMN-01 and ADMN-02 work correctly. The checkpoint confirms the entire phase delivers what was planned.

Output: Seed script with catalog data, verified admin CRUD for brands, categories, and listings.
</objective>

<execution_context>
@/Users/billy.ferris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billy.ferris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-catalog-management/02-CONTEXT.md
@.planning/phases/02-admin-catalog-management/02-01-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-02-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-03-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-04-SUMMARY.md
@.planning/phases/02-admin-catalog-management/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend seed script with brands, categories, listings, and SKUs</name>
  <files>apps/api/src/db/seed.ts</files>
  <action>
Extend the existing seed script (apps/api/src/db/seed.ts) to populate catalog data AFTER the existing companies and users are seeded. The existing seed logic must remain intact -- add catalog seeding as a new section after user creation.

**Cleanup section:** Add deletion of new tables in correct FK order at the top of the cleanup phase (before existing user/company cleanup):
1. Delete from listing_categories (join table, no FK dependencies on it)
2. Delete from brand_listing_images
3. Delete from inventory_skus
4. Delete from brand_listings
5. Delete from brands
6. Delete from categories
Then proceed with existing user and company cleanup.

**Seed categories:** Insert the 7 CPG categories from the CPG_CATEGORIES constant (imported from @ais/shared/constants). These are: Beauty Tools, Fragrances, Hair Care, Health & Wellness, Makeup, Nail Care, Skincare. Auto-generate slugs from names.

**Seed brands:** Create 3 brands, one for each manufacturer company:
1. "Summit Home Collection" (Summit Home Appliances company, companyIndex 1) -- slug: summit-home-collection
2. "FreshGlow Beauty" (FreshGlow Skincare company, companyIndex 2) -- slug: freshglow-beauty
3. "GlowPro Essentials" (FreshGlow Skincare company, companyIndex 2) -- slug: glowpro-essentials
Each brand gets a description. No logo URLs needed (placeholder fallback).

**Seed listings:** Create 4-5 brand listings across the brands:
1. "Premium Hair Care Bundle" (brand: FreshGlow Beauty, status: active) -- assigned to Hair Care, Skincare categories
2. "Professional Beauty Tools Set" (brand: GlowPro Essentials, status: active) -- assigned to Beauty Tools, Makeup categories
3. "Luxury Fragrance Collection" (brand: FreshGlow Beauty, status: draft) -- assigned to Fragrances category
4. "Summit Kitchen Essentials" (brand: Summit Home Collection, status: active) -- assigned to Health & Wellness category
5. "Seasonal Nail Art Kit" (brand: GlowPro Essentials, status: active) -- assigned to Nail Care, Makeup categories

Each listing gets a description.

**Seed SKUs:** Create 2-3 SKUs per listing (10-15 total). Each SKU should have realistic CPG data:
- name, description, upc (random 12-digit string), size (e.g., "8 oz", "12 pack"), casePack (e.g., 6, 12, 24), price (e.g., "24.99"), msrp (higher than price, e.g., "34.99"), quantity (50-500), expirationDate (some future date for applicable items, null for non-perishable).
- No imageUrls needed (placeholder fallback).

**Seed listing_categories:** Insert the many-to-many relationships as defined above for each listing.

**Do NOT seed brand_listing_images** -- images require R2 URLs which may not be set up. Placeholder fallback handles this.

**Print summary:** At the end, log counts: categories seeded, brands seeded, listings seeded, SKUs seeded.

Import all new table schemas from the schema index. Use the same db connection instance.
  </action>
  <verify>
    <automated>cd /Users/billy.ferris/Desktop/gh-repos/ais-marketplace/apps/api && pnpm tsx src/db/seed.ts 2>&1 | tail -20 && npx vitest run src/__tests__/seed/categories.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Seed script runs without errors, populates 7 categories, 3 brands, 5 listings, and 10-15 SKUs. Existing company and user seed data is preserved. Console output shows all counts. Seed test stub runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete admin catalog management experience</name>
  <action>
Human verifies the complete admin catalog management system built across Phase 2.

What was built:
- Database schema with brands, categories, brand_listings, inventory_skus, listing_categories, brand_listing_images tables
- React Router with /manage/brands, /manage/categories, /manage/listings routes
- Collapsible Manage sub-navigation in sidebar
- Brand and category CRUD with dialog modals, data tables, search, soft-delete
- Listing CRUD with dedicated create/edit pages, inline SKU editing, category assignment, image upload
- Seed data populating demo brands, categories, listings, and SKUs
  </action>
  <verify>
Start both servers if not running:
1. Terminal 1: `cd apps/api && pnpm dev`
2. Terminal 2: `cd apps/web && pnpm dev`

Sign in as admin (admin@ais-demo.com / AisMarket2026!)

**Sidebar Navigation:**
3. Verify sidebar shows "Manage" item that expands to show Brands, Categories, Listings sub-items
4. Click each sub-item and verify URL changes to /manage/brands, /manage/categories, /manage/listings

**Brands Management (/manage/brands):**
5. Verify seeded brands appear in the data table (Summit Home Collection, FreshGlow Beauty, GlowPro Essentials)
6. Click "Add Brand" -- verify dialog opens with name, description, company select, logo upload fields
7. Create a test brand -- verify it appears in the table, toast shows success
8. Click edit on the test brand -- verify dialog pre-populates, make a change, save
9. Click delete on the test brand -- verify confirmation dialog, confirm, verify it disappears

**Categories Management (/manage/categories):**
10. Verify 7 CPG categories appear in the table
11. Create a test category, edit it, delete it -- same flow as brands

**Listings Management (/manage/listings):**
12. Verify seeded listings appear with status badges, brand names, SKU counts
13. Test search and status filter
14. Click "Create Listing" -- verify navigates to /manage/listings/new
15. On create page: fill in name, select brand, set status, check categories, add 2 SKUs with name/price/quantity
16. Submit and verify redirect to listings table with new listing shown
17. Click edit on the new listing -- verify navigates to edit page with all data pre-populated
18. Edit a field, add another SKU, save -- verify changes persist
19. Delete a listing from the table -- verify confirmation and removal

**Persistence Check:**
20. Refresh the browser -- verify all data persists (not lost on reload)
  </verify>
  <done>Admin can fully manage brands, categories, and listings through the UI. All CRUD operations work. Seeded data appears correctly. Phase 2 requirements ADMN-01 and ADMN-02 are satisfied.</done>
</task>

</tasks>

<verification>
- Seed script runs cleanly and populates demo data
- All three management pages render with seeded data
- Full CRUD operations work for brands, categories, and listings
- Inline SKU editing works on listing create and edit pages
- Soft-delete removes items from all list views
- Data persists across page refresh
- Toast notifications confirm all operations
- No console errors in browser or API server
- `cd apps/api && pnpm test` passes (all stubs report todos)
</verification>

<success_criteria>
- ADMN-01 satisfied: Admin can create, edit, and delete inventory listings with all product fields
- ADMN-02 satisfied: Admin can manage brands and categories (create, edit, delete, view in tables)
- Seed data provides realistic demo environment
- Phase 2 success criteria met:
  1. Admin can create a new inventory listing with all fields and it persists in PostgreSQL
  2. Admin can edit and delete existing listings, changes reflected immediately
  3. Admin can create, edit, and delete brands and categories, and assign listings to them
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-catalog-management/02-06-SUMMARY.md`
</output>
