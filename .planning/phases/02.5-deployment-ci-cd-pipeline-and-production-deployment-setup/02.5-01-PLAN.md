---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - eslint.config.js
  - turbo.json
  - package.json
  - apps/api/package.json
  - apps/web/package.json
autonomous: true
requirements: [D-03, D-10]
must_haves:
  truths:
    - "`pnpm turbo run lint typecheck` executes >0 tasks and exits 0"
    - "eslint actually lints source (no longer 'No tasks were executed')"
    - "`NODE_ENV=production pnpm --filter @ais/api start` boots and serves /api/health without ERR_MODULE_NOT_FOUND"
    - "tsx and drizzle-kit survive a production (devDep-pruned) install"
  artifacts:
    - path: "eslint.config.js"
      provides: "Flat eslint config covering apps/* + packages/*"
      contains: "typescript-eslint"
    - path: "turbo.json"
      provides: "typecheck turbo task"
      contains: "typecheck"
    - path: "apps/api/package.json"
      provides: "lint + typecheck scripts; tsx & drizzle-kit as dependencies; tsx-based start"
      contains: "tsx src/index.ts"
  key_links:
    - from: "turbo.json typecheck/lint tasks"
      to: "per-package lint/typecheck scripts"
      via: "turbo run <task> fans out to package scripts"
      pattern: "\"(lint|typecheck)\""
    - from: "apps/api start script"
      to: "tsx runtime"
      via: "run-from-source (no dist build)"
      pattern: "tsx src/index.ts"
---

<objective>
Make the D-03 CI gate real and unbreak the production start path — both are Wave 0 unblockers every later plan depends on.

Today `turbo run lint` runs 0 tasks (no eslint config, no lint scripts) so the D-03 lint gate is vaporware, and `node dist/index.js` (the current prod `start`) crashes on boot with `ERR_MODULE_NOT_FOUND` (extensionless ESM imports + `@ais/shared` shipping raw `.ts`). This plan installs a flat eslint toolchain, adds `lint`/`typecheck` scripts + turbo tasks across the workspace, promotes `tsx` + `drizzle-kit` to runtime dependencies, and repoints the API prod start to the verified-working `tsx src/index.ts`.

Purpose: without these, the CI workflow (Plan 03) gates nothing and the Railway deploy (Plan 05) enters a restart loop.
Output: working `pnpm turbo run lint typecheck`, a bootable production start command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install eslint flat config + typecheck turbo task</name>
  <files>eslint.config.js, turbo.json, package.json</files>
  <read_first>
    - RESEARCH.md §"Code Examples" lines 324-347 (flat config shape, turbo task shapes) and §"Standard Stack → Supporting (Wave 0)" lines 79-99 (install command, package set).
    - PATTERNS.md §"eslint.config.js (GREENFIELD)" lines 101-114 and §"turbo.json — add typecheck task" lines 232-244 and §"root package.json" lines 248-254.
    - CONTEXT.md D-03 (eslint + tsc --noEmit gate).
    - Current `turbo.json` `lint` task shape (lines 12-14); current root `package.json` scripts (lines 3-11) and devDependencies (turbo only).
  </read_first>
  <action>
    Install the eslint toolchain at the workspace root as dev dependencies: `eslint`, `@eslint/js`, and `typescript-eslint` via `pnpm add -Dw eslint @eslint/js typescript-eslint` (verify the exact current major set at install time per RESEARCH Package Legitimacy note). Create `eslint.config.js` at the repo root as an eslint 9 flat config using the `typescript-eslint` helper, composing `js.configs.recommended` and `tseslint.configs.recommended`, with an `ignores` entry covering `**/dist/**`, `**/node_modules/**`, and `apps/api/drizzle/**` (the generated migrations dir Plan 02 creates). Add a `typecheck` task to `turbo.json` as a sibling of the existing `lint` task, with `dependsOn: ["^build"]` so `@ais/shared`'s type surface is available. Add a `typecheck` passthrough script to the root `package.json` (`turbo run typecheck`) alongside the existing `lint`/`test` passthroughs. `apps/web` has never been linted — scope or downgrade noisy web rules (add targeted `ignores`/rule overrides) so the config runs clean rather than blocking this phase on a web lint cleanup; do NOT weaken API rules.
  </action>
  <verify>eslint runs against a package without crashing: `pnpm --filter @ais/api exec eslint src` completes (exit 0 after tuning); `node -e "require('fs').existsSync('eslint.config.js')||process.exit(1)"`.</verify>
  <acceptance_criteria>
    - `eslint.config.js` exists at repo root and imports `typescript-eslint`.
    - `turbo.json` contains a `typecheck` task with `dependsOn: ["^build"]`.
    - Root `package.json` has a `typecheck` script running `turbo run typecheck`.
    - Root `devDependencies` include `eslint`, `@eslint/js`, `typescript-eslint`.
  </acceptance_criteria>
  <done>Flat eslint config present, typecheck turbo task + root script wired, eslint toolchain installed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-package lint/typecheck scripts + fix prod start</name>
  <files>apps/api/package.json, apps/web/package.json</files>
  <read_first>
    - RESEARCH.md §"Pitfall 1" lines 286-290 (broken `node dist/index.js`, verified fix `tsx src/index.ts`) and §"Pitfall 2" lines 292-296 (NODE_ENV=production prunes tsx/drizzle-kit → promote to dependencies).
    - PATTERNS.md §"apps/api/package.json — scripts + dependency promotion" lines 206-217 and §"apps/web/package.json" lines 221-228.
    - CONTEXT.md D-10 (Nixpacks build; explicit start `pnpm --filter @ais/api start`) and D-03.
    - Current `apps/api/package.json` scripts (lines 6-14), dependencies (15-28), devDependencies (29-37); current `apps/web/package.json` scripts (lines 6-10).
  </read_first>
  <action>
    In `apps/api/package.json`: add a `typecheck` script (`tsc --noEmit`) and a `lint` script (`eslint src`); move `drizzle-kit` and `tsx` from `devDependencies` to `dependencies` (per Pitfall 2, so `NODE_ENV=production` install does not prune the tools `preDeployCommand` and the start command need); repoint the `start` script from the broken `node dist/index.js` to `tsx src/index.ts` (the verified-working run-from-source path — no `dist` build). In `apps/web/package.json`: add a `typecheck` script (`tsc -b --noEmit`, because web uses project-references) and a `lint` script (`eslint src`). Do not alter existing `dev`/`build`/`test`/`db:*` scripts. After editing, run a frozen-lockfile-safe install so the moved deps re-resolve.
  </action>
  <verify>`pnpm install` then `pnpm turbo run lint typecheck` exits 0 and reports >0 tasks; `NODE_ENV=production pnpm --filter @ais/api start &` then `curl -s localhost:3001/api/health` returns `{"status":"ok"}` (kill the process after).</verify>
  <acceptance_criteria>
    - `apps/api/package.json` `start` is `tsx src/index.ts`; `dependencies` include `tsx` and `drizzle-kit`; `devDependencies` no longer list them.
    - `apps/api` and `apps/web` each have `lint` and `typecheck` scripts.
    - `pnpm turbo run lint typecheck` exits 0 and does NOT print "No tasks were executed".
    - `NODE_ENV=production pnpm --filter @ais/api start` serves `/api/health` → `{"status":"ok"}` (no `ERR_MODULE_NOT_FOUND`).
  </acceptance_criteria>
  <done>Lint/typecheck gate runs real tasks across the workspace; production start boots from source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | New dev packages (eslint toolchain) enter the build |
| build/install → prod runtime | devDep pruning under NODE_ENV=production can remove tools the runtime needs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-01-SC | Tampering | `pnpm add -Dw eslint @eslint/js typescript-eslint` | accept | All three are first-party ESLint org / typescript-eslint packages with a legitimacy row in RESEARCH.md §"Package Legitimacy Audit" (no [ASSUMED]/[SUS]/[SLOP]); no install checkpoint required. Verify exact majors at install. |
| T-025-01-E | Elevation of Privilege | prod runtime missing `tsx`/`drizzle-kit` after prune | mitigate | Promote both to `dependencies` so a pruned production install still resolves them (RESEARCH Pitfall 2). |
</threat_model>

<verification>
- `pnpm turbo run lint typecheck` exits 0 with >0 tasks executed.
- Production start (`NODE_ENV=production pnpm --filter @ais/api start`) boots and answers `/api/health`.
- `apps/api` dependencies list `tsx` + `drizzle-kit` (not devDependencies).
</verification>

<success_criteria>
The D-03 lint/typecheck gate executes real work and the production start path boots — the two Wave 0 landmines RESEARCH verified are cleared, unblocking Plans 03 and 05.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-01-SUMMARY.md` when done.
</output>
