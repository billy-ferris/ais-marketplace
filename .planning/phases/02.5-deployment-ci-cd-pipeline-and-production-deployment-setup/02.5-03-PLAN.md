---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 03
type: execute
wave: 1
depends_on: ["02.5-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [D-03, D-04]
must_haves:
  truths:
    - "CI runs on push to main and on every pull_request"
    - "CI executes turbo run typecheck lint test (the full D-03 gate)"
    - "CI needs no Postgres service (tests are fully mocked)"
    - "main is branch-protected: the CI check must pass before merge"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI gate"
      contains: "turbo run"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "turbo tasks (typecheck lint test)"
      via: "pnpm turbo run"
      pattern: "turbo run typecheck lint test"
    - from: "branch protection on main"
      to: "CI status check"
      via: "required status check"
      pattern: "ci"
---

<objective>
Stand up the GitHub Actions CI gate (D-03) and protect `main` with it (D-04). The workflow runs `turbo run typecheck lint test` on push + PR using the repo's pinned pnpm, with no Postgres (the 171 API tests are fully mocked and pass with an invalid DATABASE_URL).

Purpose: enforce quality on every change before it reaches `main` and, transitively, production.
Output: `.github/workflows/ci.yml` + branch protection requiring the CI check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md

# Depends on Plan 01: the `typecheck` + `lint` turbo tasks/scripts must exist for the workflow to invoke them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - RESEARCH.md §"Pattern 1: CI workflow" lines 169-193 (exact YAML shape; ordering constraint; no TURBO_TOKEN) and §"Validation Architecture → DB required? No" lines 441 (mocked tests, no Postgres).
    - PATTERNS.md §".github/workflows/ci.yml (GREENFIELD)" lines 35-65 (repo facts: pnpm@9.15.0 pin, turbo tasks, no service containers).
    - CONTEXT.md D-03 (eslint + tsc --noEmit + full API suite; pnpm@9.15.0; turbo tasks) and D-04 (branch protection gates main).
    - Root `package.json` `packageManager: pnpm@9.15.0` pin.
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` following RESEARCH Pattern 1: trigger on `push` to `main` and on all `pull_request`; add a `concurrency` group keyed on `github.ref` with `cancel-in-progress: true`; a single `verify` job on `ubuntu-latest` with a 10-minute timeout. Steps in order: `actions/checkout@v4`, then `pnpm/action-setup@v4` with NO hardcoded `version:` (it reads the `pnpm@9.15.0` pin from root `package.json`), then `actions/setup-node@v4` with `node-version: 22` and `cache: 'pnpm'`, then `pnpm install --frozen-lockfile`, then `pnpm turbo run typecheck lint test`. The `pnpm/action-setup` step MUST precede `setup-node` so the pnpm-store cache resolves. Do NOT add Postgres service containers (tests are mocked). Do NOT set `TURBO_TOKEN`/`TURBO_TEAM` (local-cache-only CI). Do NOT add any secrets — the mocked suite needs none.
  </action>
  <verify>Workflow is valid YAML and references the three tasks: `grep -Eq 'turbo run typecheck lint test' .github/workflows/ci.yml` and `grep -q 'pnpm/action-setup@v4' .github/workflows/ci.yml`.</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` triggers on `push` (main) + `pull_request`.
    - It runs `pnpm turbo run typecheck lint test`.
    - `pnpm/action-setup@v4` appears BEFORE `actions/setup-node@v4`; setup-node uses `cache: 'pnpm'`.
    - No service containers, no `TURBO_TOKEN`/`TURBO_TEAM`, no secrets referenced.
  </acceptance_criteria>
  <done>CI workflow committed; on the next push/PR it runs the full lint+typecheck+test gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable branch protection on main (GitHub UI)</name>
  <what-built>The CI workflow from Task 1 that produces a required status check.</what-built>
  <how-to-verify>
    1. Push this branch and open a PR so GitHub registers the `verify` check name at least once (the status check must have run to be selectable).
    2. In GitHub → repo → Settings → Branches → Branch protection rules → Add rule for `main`.
    3. Enable "Require a pull request before merging" and "Require status checks to pass before merging"; select the CI job (`verify`) as a required check. Enable "Require branches to be up to date before merging".
    4. Save. Confirm: opening a PR with a failing lint/type/test now blocks the merge button until CI is green.
  </how-to-verify>
  <resume-signal>Type "approved" once branch protection requires the CI check on `main`, or describe what the GitHub UI showed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → main | Untrusted code changes attempt to reach the production branch |
| CI runner → secrets | Workflow runs on GitHub-hosted runners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-03-T | Tampering | merging unverified code to main | mitigate | Branch protection makes the CI check a required gate; up-to-date-branch rule prevents stale merges (D-04). |
| T-025-03-I | Information Disclosure | leaking secrets into CI logs | mitigate | Mocked test suite needs no secrets; workflow references none and sets no turbo remote-cache tokens. |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists and runs `turbo run typecheck lint test` on push + PR.
- Branch protection on `main` requires the CI status check (manual GitHub confirmation).
</verification>

<success_criteria>
Every PR runs the real lint+typecheck+test gate and cannot merge to `main` red — the D-03/D-04 quality gate is live.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-03-SUMMARY.md` when done.
</output>
