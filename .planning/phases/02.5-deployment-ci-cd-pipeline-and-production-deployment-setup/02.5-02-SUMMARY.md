---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 02
subsystem: database
tags: [drizzle, drizzle-kit, postgres, migrations, deployment]

# Dependency graph
requires:
  - phase: 02.4-security-hardening
    provides: DB-level data-integrity constraints (partial unique indexes, FK onDelete) captured in the baseline schema
provides:
  - Committed baseline drizzle migration (0000_petite_scarlet_witch.sql) representing the full current schema
  - Drizzle migration journal (meta/_journal.json) + snapshot (meta/0000_snapshot.json) for applied-migration tracking
  - Verified generate → commit → migrate production flow (D-05/D-06) that retires db:push for prod
affects: [02.5-05-railway-deployment, preDeployCommand, database-migrations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Production schema changes flow: edit schema → drizzle-kit generate → commit reviewable SQL → prod applies via drizzle-kit migrate"
    - "db:push retained as dev-only convenience; never the prod schema mechanism"

key-files:
  created:
    - apps/api/drizzle/0000_petite_scarlet_witch.sql
    - apps/api/drizzle/meta/_journal.json
    - apps/api/drizzle/meta/0000_snapshot.json
  modified: []

key-decisions:
  - "Committed drizzle-kit generate output verbatim (no hand edits) as the baseline migration"
  - "db:push remains dev-only; prod uses drizzle-kit migrate (D-05/D-06)"

patterns-established:
  - "Baseline migration: full current schema captured in 0000_*.sql, applied once against fresh prod Postgres"
  - "Idempotent migrate: journal tracking means re-runs (deploy retries) do not double-apply"

requirements-completed: [D-05, D-06]

# Metrics
duration: 3min
completed: 2026-07-06
---

# Phase 02.5 Plan 02: Baseline Drizzle Migration Summary

**Committed a verbatim `drizzle-kit generate` baseline (9 tables incl. Phase 02.4 partial unique indexes) and proved `drizzle-kit migrate` applies cleanly once and is idempotent on re-run against a fresh Postgres — giving Plan 05's Railway pre-deploy step a real, reviewable schema to apply.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-06T01:28:52Z
- **Completed:** 2026-07-06T01:31:56Z
- **Tasks:** 2
- **Files modified:** 3 (all created)

## Accomplishments
- Generated baseline `0000_petite_scarlet_witch.sql` from the current schema (`apps/api/src/db/schema/index.ts`) — 9 tables, committed as unmodified tool output
- Baseline captures all Phase 01/02.x tables plus the Phase 02.4 partial unique indexes (`brands_slug_active_unique`, `categories_slug_active_unique`, `brand_listing_images_one_primary_per_listing`)
- Verified `drizzle-kit migrate` applies cleanly against a fresh/empty Postgres (9 tables created, 1 journal entry, no "relation already exists")
- Verified idempotency: a second consecutive `drizzle-kit migrate` exits 0, applies nothing (still 9 tables, journal still 1 entry — no double-apply)
- Established the production migration flow (generate → commit → migrate) that retires `db:push` for prod (D-05/D-06)

## Task Commits

1. **Task 1: Generate + commit the baseline drizzle migration** - `517ddef` (feat)
2. **Task 2: Verify migrate applies cleanly + idempotently on a fresh DB** - verification-only, no committable artifacts (`drizzle-kit migrate` creates no files; it writes to the target DB's journal)

## Files Created/Modified
- `apps/api/drizzle/0000_petite_scarlet_witch.sql` - Baseline SQL migration (9 CREATE TABLE + partial unique indexes) for the current schema
- `apps/api/drizzle/meta/_journal.json` - Drizzle migration journal (version 7, one entry: `0000_petite_scarlet_witch`)
- `apps/api/drizzle/meta/0000_snapshot.json` - Drizzle schema snapshot backing the baseline migration

## Decisions Made
- Left `apps/api/drizzle.config.ts` unchanged — it already targets `out: './drizzle'`, `schema: './src/db/schema/index.ts'`, dialect `postgresql`; default `migrations.table` is fine (near-noop as planned).
- `db:push` is retained ONLY as a dev convenience and is NOT the prod schema mechanism (D-05). Prod applies the committed baseline via `drizzle-kit migrate` (to be wired into Railway `preDeployCommand` in Plan 05, D-06).
- Did NOT run `db:seed` during verification (Phase 02.4 `assertNotProduction()` guard + migrate-only deploy path forbid seeding).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed workspace dependencies in the fresh worktree**
- **Found during:** Task 1 (baseline generation)
- **Issue:** The worktree had no `node_modules`, so `drizzle-kit` was unavailable (`Command "drizzle-kit" not found`), blocking `drizzle-kit generate`.
- **Fix:** Ran `pnpm install --frozen-lockfile` — installs only already-declared, lockfile-pinned dependencies (no new/added packages; not a package-manager add). `drizzle-kit@^0.31.0` is already declared in `apps/api/package.json`.
- **Files modified:** None tracked (`node_modules` is gitignored).
- **Verification:** `drizzle-kit generate` subsequently ran successfully (exit 0).
- **Committed in:** N/A (no tracked changes — dependency install only).

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to run the drizzle tooling in a fresh worktree. Frozen-lockfile install of pre-declared deps only — no new packages, no scope creep.

## Issues Encountered
- **Docker daemon down:** The plan suggested a disposable container for the fresh-DB verification, but the Docker daemon was not running. Resolved by using the running local Homebrew `postgresql@15` server: created a throwaway scratch database (`ais_migrate_verify_<pid>`), ran both migrate passes against it via a scoped `DATABASE_URL`, then dropped it. No `db:seed`/`db:push` was run against the target. The project's real databases (`ais_marketplace`) were untouched.

## Verification Evidence
- First `drizzle-kit migrate` against empty scratch DB: exit 0, `public` schema went 0 → 9 tables, `drizzle.__drizzle_migrations` = 1 entry, no "relation already exists".
- Second consecutive `drizzle-kit migrate`: exit 0, tables still 9, journal still 1 entry (idempotent, no double-apply).
- Scratch DB dropped after verification; no residual state.

## Next Phase Readiness
- Baseline migration is committed and proven clean + idempotent — ready for Plan 05 to gate it behind the Railway `preDeployCommand` so the API never serves an un-migrated DB (D-06).
- No blockers. Note for the human: verification used a local Homebrew Postgres (Docker daemon was down); the same commands should apply identically in CI/Railway's Postgres.

## Self-Check

- **Created files:**
  - FOUND: apps/api/drizzle/0000_petite_scarlet_witch.sql
  - FOUND: apps/api/drizzle/meta/_journal.json
  - FOUND: apps/api/drizzle/meta/0000_snapshot.json
- **Commit:** FOUND: 517ddef (feat(02.5-02): add baseline drizzle migration from current schema)

## Self-Check: PASSED

---
*Phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup*
*Completed: 2026-07-06*
