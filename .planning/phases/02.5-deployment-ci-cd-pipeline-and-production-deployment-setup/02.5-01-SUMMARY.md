---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 01
subsystem: build-tooling
tags: [eslint, turbo, typecheck, ci-gate, prod-start, tsx]
requires:
  - "monorepo turbo pipeline"
  - "apps/api Express entrypoint (src/index.ts)"
provides:
  - "Working `pnpm turbo run lint typecheck` gate (5 real tasks, exit 0)"
  - "Bootable production start: `tsx src/index.ts` (no dist build)"
  - "eslint flat config covering apps/* + packages/*"
affects:
  - "Plan 03 (CI workflow — now has a real lint/typecheck gate to run)"
  - "Plan 05 (Railway deploy — start command no longer restart-loops)"
tech-stack:
  added:
    - "eslint@10.6 (flat config)"
    - "@eslint/js@10.0"
    - "typescript-eslint@8.62"
    - "eslint-plugin-react-hooks@7.1"
  patterns:
    - "ESLint 9/10 flat config via tseslint.config() helper"
    - "Run-from-source prod start with tsx (no tsc/dist artifact)"
    - "Runtime tools (tsx, drizzle-kit) as dependencies to survive NODE_ENV=production prune"
key-files:
  created:
    - "eslint.config.js"
  modified:
    - "turbo.json"
    - "package.json"
    - "apps/api/package.json"
    - "apps/web/package.json"
    - "packages/shared/package.json"
    - "apps/api/src/db/seed.ts"
    - "apps/api/src/routes/listings.ts"
decisions:
  - "eslint installed at v10 (not v9); flat config is identical and typescript-eslint 8.x supports it"
  - "Honor repo-wide `_`-prefix convention via argsIgnorePattern/varsIgnorePattern/caughtErrorsIgnorePattern rather than weakening no-unused-vars"
  - "Relax no-explicit-any/no-unused-vars for test files only (vitest mock patterns) — production API rules unchanged"
  - "Installed eslint-plugin-react-hooks (beyond plan's 3-package list) so the web block's react-hooks/exhaustive-deps override loads"
metrics:
  tasks: 2
  files_changed: 8
  completed: 2026-07-05
---

# Phase 02.5 Plan 01: CI Gate + Prod Start Unblock Summary

Made the D-03 lint/typecheck gate real (`pnpm turbo run lint typecheck` now runs 5 tasks and exits 0) and unbroke the production start path by repointing it to the verified-working `tsx src/index.ts` — clearing the two Wave 0 landmines that Plans 03 and 05 depend on.

## What Was Built

**Task 1 — eslint flat config + typecheck turbo task**
- Installed the eslint 10 flat toolchain at the workspace root: `eslint`, `@eslint/js`, `typescript-eslint` (plus `eslint-plugin-react-hooks`, see Deviations).
- Created `eslint.config.js` (repo root) composing `js.configs.recommended` + `tseslint.configs.recommended`, with `ignores` for `**/dist/**`, `**/node_modules/**`, `apps/api/drizzle/**`.
- Added a `typecheck` turbo task (`dependsOn: ["^build"]`) as a sibling of `lint`, and a `typecheck` passthrough script to the root `package.json`.
- Pinned `engines.node = "22.x"` on the root `package.json` so CI (Plan 03), Railway (Plan 05), and local dev agree on a Node major (closes RESEARCH Assumption A6).

**Task 2 — per-package scripts + prod start fix**
- `apps/api`: added `lint` (`eslint src`) + `typecheck` (`tsc --noEmit`); repointed `start` from the broken `node dist/index.js` to `tsx src/index.ts`; promoted `tsx` and `drizzle-kit` from `devDependencies` to `dependencies` (Pitfall 2 — so a `NODE_ENV=production` install does not prune them).
- `apps/web`: added `lint` (`eslint src`) + `typecheck` (`tsc -b --noEmit`, project-references aware).
- `packages/shared`: added `typecheck` (`tsc --noEmit`) — it has no `build` script, so the turbo task's `^build` dep would otherwise silently skip it.

## Exact ESLint Overrides Chosen (plan required recording these)

| Scope | Rule | Level | Why |
|-------|------|-------|-----|
| Global | `@typescript-eslint/no-unused-vars` | `error` + `{args/vars/caughtErrors IgnorePattern: '^_'}` | Honors the existing `_`-prefix convention (e.g. `_next` Express error arg, destructured-and-dropped fields). Not a weakening. |
| Test files (`**/__tests__/**`, `**/*.test.ts(x)`) | `@typescript-eslint/no-explicit-any` | `off` | vitest mocks legitimately use `any` (thenable mocks, `vi.hoisted` factories) — 157 occurrences, all in test code. |
| Test files | `@typescript-eslint/no-unused-vars` | `off` | Mock/spy scaffolding. |
| Web (`apps/web/**/*.{ts,tsx}`) | `no-unused-vars` | `warn` | apps/web never linted; surface without blocking CI. |
| Web | `@typescript-eslint/no-unused-vars` | `warn` | same |
| Web | `@typescript-eslint/no-explicit-any` | `warn` | same |
| Web | `react-hooks/exhaustive-deps` | `warn` | same (requires react-hooks plugin) |

Production API source rules were NOT weakened.

## Deviations from Plan

### Auto-fixed / Auto-added

**1. [Rule 3 - Blocking] Installed `eslint-plugin-react-hooks` (beyond the plan's 3-package install list)**
- **Found during:** Task 1
- **Issue:** The plan directs the web config block to downgrade `react-hooks/exhaustive-deps` to `warn`, but referencing a rule from an unregistered plugin makes ESLint hard-fail config loading ("Could not find plugin 'react-hooks'"). The plan's install command only listed `eslint @eslint/js typescript-eslint`.
- **Fix:** Installed `eslint-plugin-react-hooks@7.1` (first-party React org package) as a dev dependency and registered it in the web-scoped config block.
- **Files modified:** `package.json`, `pnpm-lock.yaml`, `eslint.config.js`
- **Commit:** 6c46bd1

**2. [Rule 1 - Bug] Fixed genuine source lint errors so `eslint src` exits 0**
- **Found during:** Task 1 tuning (plan verify requires exit 0 "after tuning")
- **Issue:** Two real (non-noise) findings in production source: unused `eq` import in `seed.ts`; `prefer-const` violation (`let listingBrandId`, never reassigned) in `listings.ts`.
- **Fix:** Removed the unused import; changed `let` → `const`.
- **Files modified:** `apps/api/src/db/seed.ts`, `apps/api/src/routes/listings.ts`
- **Commit:** 6c46bd1

**3. [Config tuning] `_`-prefix ignore + test-file relaxation**
- Documented in the overrides table above. This is the "tuning" the plan's Task 1 verify anticipates; it reaches `eslint src` exit 0 without weakening production API source rules (the 170 initial errors were 157 `any` in tests, 5 unused-var in tests, 6 intentional `_`-prefixed source vars, and 2 genuine source bugs).

### Notes (non-deviations)

- ESLint resolved to **v10.6** (not the v9 RESEARCH anticipated). Flat config is identical; `typescript-eslint@8.x` supports both. No action needed.
- The `engines.node: "22.x"` pin emits a non-blocking `Unsupported engine` warning locally (dev machine is on Node v24). This is expected and intentional — pnpm does not enforce engines (engine-strict off). CI and Railway will run Node 22.
- Two harmless `warning` lines remain on `apps/api/src/__tests__/seed/guard.test.ts` (now-redundant `eslint-disable` directives, since `no-explicit-any` is off for tests). Warnings do not affect exit 0; left untouched (pre-existing test code, out of scope).

## Verification

- `pnpm turbo run lint typecheck` → **5 successful, 5 total, exit 0** (api:lint, api:typecheck, web:lint, web:typecheck, shared:typecheck). No "No tasks were executed".
- `pnpm --filter @ais/api exec eslint src` → exit 0 (0 errors).
- `NODE_ENV=production` `tsx src/index.ts` boot → log shows `API server running on port <p>`, and `GET /api/health` → `{"status":"ok"}`. **No `ERR_MODULE_NOT_FOUND`.** (Verified on a free port with a validly-formatted dummy Clerk key; the default port 3001 was occupied by the user's running dev server.)
- `apps/api/package.json`: `dependencies` now include `tsx` + `drizzle-kit`; `devDependencies` no longer list them.

## Must-Haves Confirmed

- [x] `pnpm turbo run lint typecheck` executes >0 tasks and exits 0
- [x] eslint actually lints source (no longer "No tasks were executed")
- [x] `NODE_ENV=production` start boots and serves `/api/health` without `ERR_MODULE_NOT_FOUND`
- [x] `tsx` + `drizzle-kit` promoted to `dependencies` (survive a devDep-pruned production install)
