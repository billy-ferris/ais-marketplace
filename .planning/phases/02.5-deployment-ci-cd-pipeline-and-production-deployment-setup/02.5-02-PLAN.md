---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/drizzle/
  - apps/api/drizzle.config.ts
autonomous: true
requirements: [D-05, D-06]
must_haves:
  truths:
    - "A committed baseline SQL migration exists under apps/api/drizzle/ representing the current schema"
    - "`drizzle-kit migrate` applies the baseline cleanly against a fresh/empty Postgres"
    - "Re-running `drizzle-kit migrate` is idempotent (no error, nothing re-applied)"
    - "db:push is retained ONLY as a dev convenience, never the prod schema mechanism"
  artifacts:
    - path: "apps/api/drizzle/meta/_journal.json"
      provides: "Drizzle migration journal tracking applied migrations"
      contains: "version"
    - path: "apps/api/drizzle.config.ts"
      provides: "Migration output + schema paths (out: ./drizzle)"
      contains: "out"
  key_links:
    - from: "apps/api/src/db/schema/index.ts"
      to: "apps/api/drizzle/0000_*.sql"
      via: "drizzle-kit generate"
      pattern: "CREATE TABLE"
    - from: "drizzle-kit migrate"
      to: "Postgres __drizzle_migrations journal"
      via: "applied-migration tracking"
      pattern: "drizzle"
---

<objective>
Establish the production migration flow that RETIRES `db:push` for prod (D-05/D-06): dev edits schema → `drizzle-kit generate` → commit reviewable SQL → prod applies via `drizzle-kit migrate` in a pre-deploy step. This plan produces the baseline migration from the current schema and verifies it applies cleanly + idempotently against a fresh Postgres.

Purpose: production Postgres will be brand-new/empty; without a committed baseline migration there is nothing for the Railway `preDeployCommand` (Plan 05) to apply, and the DB would be served un-migrated — a D-06 violation.
Output: committed `apps/api/drizzle/0000_*.sql` + `meta/_journal.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate + commit the baseline drizzle migration</name>
  <files>apps/api/drizzle/, apps/api/drizzle.config.ts</files>
  <read_first>
    - RESEARCH.md §"Code Examples → drizzle.config.ts" lines 350-366 (generate/migrate commands; config already migration-shaped) and §"Pitfall 4" lines 304-308 (baseline vs db:push drift; prod DB is empty so baseline applies clean).
    - PATTERNS.md §"apps/api/drizzle/0000_*.sql — TOOL-GENERATED" lines 118-120 (never hand-author; generate against configured schema path) and §"apps/api/drizzle.config.ts — NEAR-NOOP" lines 200-202.
    - CONTEXT.md D-05 (generate → commit SQL → reviewable) and D-06 (apply pre-deploy, never serve un-migrated).
    - Current `apps/api/drizzle.config.ts` (out: ./drizzle, schema: ./src/db/schema/index.ts, dialect postgresql).
  </read_first>
  <action>
    Confirm `apps/api/drizzle.config.ts` already targets `out: './drizzle'` and `schema: './src/db/schema/index.ts` (near-noop; leave as-is unless a `migrations.table` override is desired — defaults are fine). Run `pnpm --filter @ais/api exec drizzle-kit generate` to emit the baseline SQL migration plus `meta/_journal.json` into `apps/api/drizzle/`. Do NOT hand-edit the generated SQL — commit the tool output verbatim. This baseline captures the full current schema (all Phase 01/02.x tables, enums, and the Phase 02.4 partial unique indexes). Ensure `apps/api/drizzle/` is committed (it is intentionally NOT ignored by eslint runtime — Plan 01 already added it to the eslint `ignores`).
  </action>
  <verify>`ls apps/api/drizzle/*.sql` shows at least one `0000_*.sql`; `test -f apps/api/drizzle/meta/_journal.json`.</verify>
  <acceptance_criteria>
    - `apps/api/drizzle/0000_*.sql` exists and contains `CREATE TABLE` statements for the current schema.
    - `apps/api/drizzle/meta/_journal.json` exists.
    - Generated files are unmodified tool output (no manual edits).
  </acceptance_criteria>
  <done>Baseline migration generated from current schema and committed verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Verify migrate applies cleanly + idempotently on a fresh DB</name>
  <files>apps/api/drizzle/</files>
  <read_first>
    - RESEARCH.md §"Don't Hand-Roll" lines 262-265 (drizzle-kit migrate tracks journal, non-interactive, idempotent) and §"Pitfall 4" lines 304-308 (clean apply against empty DB; "relation already exists" = ran against push-shaped DB) and §"Anti-Patterns" lines 252-257 (never apply on request path, never seed in prod).
    - CONTEXT.md D-06 (migrate before serve).
  </read_first>
  <action>
    Against a scratch/empty Postgres (a local throwaway database or a disposable container — set `DATABASE_URL` to it), run `pnpm --filter @ais/api exec drizzle-kit migrate` and confirm the baseline applies without a "relation already exists" error (that error would indicate a pre-existing push-shaped DB, which prod is not). Run `drizzle-kit migrate` a SECOND time and confirm it is idempotent — reports nothing to apply, exits 0. Do NOT run `db:seed` as part of this verification (the Phase 02.4 `assertNotProduction()` guard and the migrate-only rule forbid seeding in the deploy path). Record in the SUMMARY that `db:push` remains a dev-only convenience and is never the prod schema mechanism (D-05).
  </action>
  <verify>First `drizzle-kit migrate` exits 0 and creates tables on the scratch DB; second consecutive `drizzle-kit migrate` exits 0 with no new migrations applied.</verify>
  <acceptance_criteria>
    - First `drizzle-kit migrate` against a fresh DB exits 0 and creates the schema (no "relation already exists").
    - Immediate re-run of `drizzle-kit migrate` exits 0 and applies nothing (idempotent).
    - No `db:seed`/`db:push` executed against the target during verification.
  </acceptance_criteria>
  <done>Baseline migration proven to apply cleanly once and be idempotent on re-run — safe for the Railway pre-deploy step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy lifecycle → database | Schema-changing SQL is applied to production Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-02-T | Tampering | production schema application | mitigate | Retire destructive/interactive `db:push` for prod; use reviewable generated SQL applied via non-interactive `drizzle-kit migrate` (D-05/D-06). |
| T-025-02-I | Integrity | serving an un-migrated DB | mitigate | Baseline migration exists + verified idempotent; Plan 05 gates it behind Railway `preDeployCommand` so the app never serves before migrate succeeds. |
| T-025-02-D | Denial of Service | dirty/partial migration state | mitigate | Idempotent re-run verified; drizzle journal tracks applied migrations so retries do not double-apply. |
</threat_model>

<verification>
- `apps/api/drizzle/` contains a committed baseline `.sql` + `meta/_journal.json`.
- `drizzle-kit migrate` applies clean once and is idempotent on a fresh DB.
- No prod-path seeding; `db:push` documented as dev-only.
</verification>

<success_criteria>
The prod migration mechanism (generate → commit → migrate) is real and verified, giving Plan 05's Railway pre-deploy step a clean, idempotent baseline to apply before the API serves.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-02-SUMMARY.md` when done.
</output>
