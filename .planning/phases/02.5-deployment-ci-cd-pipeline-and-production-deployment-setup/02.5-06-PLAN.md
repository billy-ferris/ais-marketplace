---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 06
type: execute
wave: 2
depends_on: ["02.5-04"]
files_modified:
  - vercel.json
  - apps/web/.env.example
autonomous: false
requirements: [D-01, D-07, D-08, D-11]
must_haves:
  truths:
    - "vercel.json rewrites all routes to /index.html (SPA deep-link routing)"
    - "The Vercel project builds apps/web with pnpm (not npm) and outputs dist"
    - "VITE_API_BASE_URL and VITE_CLERK_PUBLISHABLE_KEY are injected at build time"
    - "PR preview deploys are enabled"
  artifacts:
    - path: "vercel.json"
      provides: "SPA rewrite for client-side routing"
      contains: "index.html"
    - path: "apps/web/.env.example"
      provides: "Documented web build-time env contract (prod values note)"
      contains: "VITE_API_BASE_URL"
  key_links:
    - from: "vercel.json rewrites"
      to: "/index.html"
      via: "catch-all rewrite for client-side router"
      pattern: "index.html"
    - from: "Vercel build"
      to: "VITE_API_BASE_URL / VITE_CLERK_PUBLISHABLE_KEY"
      via: "build-time env injection"
      pattern: "VITE_"
---

<objective>
Host the Vite React SPA on Vercel (D-01/D-11): a `vercel.json` SPA rewrite (all routes → `index.html`) for client-side routing, with build-time injection of `VITE_API_BASE_URL` (→ Railway API URL) and `VITE_CLERK_PUBLISHABLE_KEY` (D-08), and PR preview deploys enabled. Provision the Vercel project with Root Directory `apps/web` and pnpm detection.

Purpose: the frontend half of the split-host topology — CDN-served SPA with automatic PR previews.
Output: committed `vercel.json` + a provisioned Vercel project (manual dashboard step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md

# Depends on Plan 04: the API CORS allowlist must exist so the deployed SPA's origin can be admitted (final origin values wired in Plan 07).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author vercel.json SPA rewrite + document web env contract</name>
  <files>vercel.json, apps/web/.env.example</files>
  <read_first>
    - RESEARCH.md §"Pattern 5: Vercel SPA (monorepo)" lines 244-250 (rewrite shape; Root Directory apps/web; pnpm detection from root lockfile + pin; VITE_* build-time injection; previews on by default) and §"Pitfall 6" lines 316-320 (npm-vs-pnpm mis-detection).
    - PATTERNS.md §"vercel.json (GREENFIELD)" lines 90-97 (rewrite shape; build output dist) and §"apps/web/.env.example — production values doc" lines 271-273 (keys unchanged; annotate prod uses pk_live + Railway URL).
    - CONTEXT.md D-11 (SPA rewrite + PR previews), D-08 (VITE_* at Vercel build time), D-07 (secrets in host store).
    - Current `apps/web/.env.example` (VITE_CLERK_PUBLISHABLE_KEY, VITE_API_BASE_URL); `apps/web/package.json` build script (`tsc -b && vite build` → dist).
  </read_first>
  <action>
    Create `vercel.json` at the repo root with a single `rewrites` entry mapping source `/(.*)` to destination `/index.html` (so client-side routes deep-link correctly — RESEARCH Pattern 5). Keep it minimal; Root Directory and framework preset are set in the Vercel UI (Task 2). In `apps/web/.env.example`, keep the existing two keys (no new keys) but annotate that production uses a `pk_live_*` publishable key and the Railway API URL for `VITE_API_BASE_URL`, injected at Vercel build time (not committed). Do not add secret values — `VITE_CLERK_PUBLISHABLE_KEY` is a publishable (public) key; no secret key belongs in the web app.
  </action>
  <verify>`grep -q 'index.html' vercel.json` and the rewrite source matches all routes; `grep -q 'VITE_API_BASE_URL' apps/web/.env.example`; `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` parses without error.</verify>
  <acceptance_criteria>
    - `vercel.json` rewrites `/(.*)` → `/index.html` and is valid JSON.
    - `apps/web/.env.example` documents the prod build-time values for `VITE_API_BASE_URL` + `VITE_CLERK_PUBLISHABLE_KEY` (no secret key present).
  </acceptance_criteria>
  <done>Committed vercel.json SPA rewrite + documented web env contract.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision the Vercel project (Vercel UI)</name>
  <what-built>The committed vercel.json (Task 1) that Vercel reads on deploy.</what-built>
  <how-to-verify>
    1. In Vercel, import the GitHub repo and set **Root Directory = `apps/web`**, framework preset **Vite**. Confirm the build log shows **pnpm** (not npm) and resolves `@ais/shared` from the root `pnpm-lock.yaml` + `packageManager: pnpm@9.15.0` pin (Pitfall 6). If it uses npm, set the install command to run at the workspace root.
    2. Set build-time env vars (D-08, host-native store): `VITE_API_BASE_URL` = the Railway API URL (from Plan 05), `VITE_CLERK_PUBLISHABLE_KEY` = the Clerk publishable key (use `pk_live_*` once the Plan 07 production instance exists). These are injected at build time only.
    3. Confirm **PR preview deploys are enabled** (default). Open a PR and confirm a preview URL builds and serves the SPA; visit a deep-link route (e.g. `/manage/approvals`) directly and confirm it returns the SPA (200 HTML), not a 404 — proving the rewrite works.
    4. Confirm the production deployment serves the SPA at the production domain.
  </how-to-verify>
  <resume-signal>Type "approved" once the Vercel project is live (pnpm build, SPA deep-links work, PR preview builds) — or describe any build-log issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time env → static bundle | VITE_* values are baked into publicly-served JS |
| CDN → browser | Static SPA served to any client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-06-I | Information Disclosure | secret key baked into the web bundle | mitigate | Only the Clerk *publishable* (public) key + API base URL are injected; NO secret key belongs in `apps/web` (verified in env contract). |
| T-025-06-SC | Tampering | wrong package manager resolving deps | mitigate | Force pnpm via root lockfile + `packageManager` pin; verify build log shows pnpm, not npm (Pitfall 6). |
| T-025-06-I2 | Information Disclosure | secrets in git | accept | `vercel.json` holds no secrets; real values in Vercel env store (D-07). |
</threat_model>

<verification>
- `vercel.json` rewrites all routes → `/index.html` (valid JSON).
- Vercel project live: pnpm build, deep-link routes serve the SPA, PR preview builds (manual confirmation).
- `VITE_*` injected at build time; no secret key in the web bundle.
</verification>

<success_criteria>
The SPA is CDN-hosted on Vercel with working deep-link routing and PR previews, built with pnpm and build-time VITE_* injection — the frontend half of the split-host topology is live.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-06-SUMMARY.md` when done.
</output>
