---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 07
type: execute
wave: 3
depends_on: ["02.5-05", "02.5-06"]
files_modified: []
autonomous: false
requirements: [D-02, D-08, D-09]
must_haves:
  truths:
    - "A Clerk production instance exists with live keys and DNS/CNAME configured"
    - "Clerk allowed_origins include the Vercel prod domain + preview pattern"
    - "Railway CORS_ALLOWED_ORIGINS + CORS_PREVIEW_PATTERN are set to the real Vercel domains"
    - "Vercel VITE_API_BASE_URL points at the real Railway API URL"
    - "End-to-end: login from the Vercel origin succeeds and authenticated API calls return 200"
  artifacts: []
  key_links:
    - from: "Clerk production instance allowed_origins"
      to: "Vercel prod domain + preview pattern"
      via: "dashboard allowlist"
      pattern: "vercel.app"
    - from: "Railway CORS_ALLOWED_ORIGINS"
      to: "API cors allowlist (Plan 04)"
      via: "runtime env consumed by buildCorsOptions()"
      pattern: "CORS_ALLOWED_ORIGINS"
    - from: "Vercel VITE_API_BASE_URL"
      to: "Railway API URL"
      via: "build-time injection consumed by apps/web/src/lib/api.ts"
      pattern: "VITE_API_BASE_URL"
---

<objective>
Complete the cross-origin production cutover now that both hosts are live (Plans 05 + 06). Create the Clerk production instance (the one no-fallback blocker RESEARCH flagged), set its allowed_origins to the Vercel domains, cross-wire the real production URLs (Railway `CORS_ALLOWED_ORIGINS` ← Vercel domains; Vercel `VITE_API_BASE_URL` ← Railway URL), redeploy, and run an end-to-end production smoke test.

Purpose: the split-host deploy only authenticates once Clerk, CORS, and the API base URL all agree on the real origins — this plan closes that loop and proves it works (D-02/D-08/D-09).
Output: a working production login + authenticated cross-origin API flow (no new committed files — all provider dashboard config + smoke verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md

# Depends on Plan 05 (Railway API URL live) + Plan 06 (Vercel domain live). Uses the CORS allowlist code from Plan 04 and authorizedParties already wired there.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Clerk production instance + cross-wire real origins</name>
  <what-built>The Plan 04 env-driven CORS allowlist + Clerk authorizedParties, and the Plan 05/06 live hosts — this task supplies them the real production origins.</what-built>
  <how-to-verify>
    1. In the Clerk dashboard, create the **production instance** for the app. Complete the required **DNS/CNAME** setup for the production Frontend API domain. Obtain the live keys (`pk_live_*` / `sk_live_*`).
    2. Set Clerk **allowed_origins** to the Vercel production domain PLUS the Vercel preview-URL pattern (so previews authenticate). (RESEARCH Pitfall 5 / Pattern 4.)
    3. In **Railway** (API service env): set `CLERK_SECRET_KEY` = `sk_live_*`, `CLERK_PUBLISHABLE_KEY` = `pk_live_*`, `CLERK_WEBHOOK_SIGNING_SECRET` = the production webhook secret; set `CORS_ALLOWED_ORIGINS` = the Vercel production domain (comma-separated if multiple) and `CORS_PREVIEW_PATTERN` = a regex matching the Vercel preview URLs (e.g. anchored `^https://<project>-[a-z0-9-]+\.vercel\.app$`); set `FRONTEND_URL` = the canonical Vercel prod URL. Confirm `NODE_TLS_REJECT_UNAUTHORIZED` is still ABSENT and `NODE_ENV=production` (D-08).
    4. In **Vercel** (web build env): set `VITE_CLERK_PUBLISHABLE_KEY` = `pk_live_*` and `VITE_API_BASE_URL` = the Railway API URL.
    5. Redeploy BOTH hosts so the new env takes effect (Vercel rebuild to bake VITE_*; Railway redeploy to pick up CORS/Clerk env).
  </how-to-verify>
  <resume-signal>Type "approved" once the Clerk production instance is live, allowed_origins include the Vercel domains, and both hosts are redeployed with the cross-wired URLs — or describe what remains.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production cross-origin end-to-end smoke test</name>
  <what-built>The fully cross-wired production deployment (Clerk prod + Railway API + Vercel SPA).</what-built>
  <how-to-verify>
    1. **Health:** `curl -s https://<railway-api-url>/api/health` returns `{"status":"ok"}`.
    2. **SPA deep-link:** `curl -sI https://<vercel-domain>/manage/approvals` returns 200 and HTML (the rewrite serves index.html), not 404.
    3. **CORS allow:** a preflight/GET from the Vercel origin returns an `Access-Control-Allow-Origin` header for that origin — e.g. `curl -sI -H "Origin: https://<vercel-domain>" https://<railway-api-url>/api/health` includes `access-control-allow-origin`.
    4. **CORS reject:** the same request with `-H "Origin: https://evil.example.com"` returns NO `access-control-allow-origin` header.
    5. **Auth end-to-end:** open the production SPA in a browser, log in via Clerk, and confirm an authenticated action (e.g. loading a page that calls the API with `Authorization: Bearer`) returns data (200), not 401 — proving `authorizedParties` + allowed_origins + live keys all agree.
    6. **Preview (D-11/D-09):** open a PR, confirm the Vercel preview URL builds and can authenticate against the API (its origin matches `CORS_PREVIEW_PATTERN` + Clerk allowed_origins).
  </how-to-verify>
  <resume-signal>Type "approved" once health, deep-link, CORS allow/reject, production login, and preview auth all pass — or paste the failing check output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel origin → Railway API | Cross-origin authenticated requests in production |
| Clerk prod instance → API/SPA | Live session issuance + verification across origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-07-S | Spoofing | Clerk token accepted from an unauthorized origin | mitigate | Clerk `allowed_origins` + API `authorizedParties` (Plan 04) both restricted to the Vercel domains; smoke test 4/5 proves reject/accept. |
| T-025-07-CORS | Information Disclosure | over-permissive CORS in prod | mitigate | Real `CORS_ALLOWED_ORIGINS`/`CORS_PREVIEW_PATTERN` set to Vercel domains only; smoke test 3/4 proves allow-known/reject-unknown. |
| T-025-07-E | Information Disclosure / MITM | dev TLS-reject or test keys leaking to prod | mitigate | Confirm `NODE_TLS_REJECT_UNAUTHORIZED` absent + `pk_live/sk_live` in use (not `pk_test`), NODE_ENV=production (D-08, Pitfall 5). |
</threat_model>

<verification>
- Clerk production instance live with allowed_origins = Vercel prod + preview.
- Real production URLs cross-wired (Railway CORS_* ← Vercel; Vercel VITE_API_BASE_URL ← Railway).
- Smoke test: health ok, deep-link 200, CORS allow known / reject unknown, prod login + preview auth succeed (manual confirmation).
</verification>

<success_criteria>
Production authentication works end-to-end across the split hosts: the Vercel SPA logs in via the Clerk production instance and makes authenticated cross-origin calls the Railway API accepts, while unknown origins are rejected — the deployment is live and secure.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-07-SUMMARY.md` when done.
</output>
