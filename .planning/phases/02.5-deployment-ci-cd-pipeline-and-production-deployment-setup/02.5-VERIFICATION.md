---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
verified: 2026-07-06T19:02:10Z
status: passed
score: 11/11 must-haves verified (artifact scope); live provisioning done 2026-07-06 — Railway API + Vercel SPA + CORS cross-wire + E2E login all verified live; 2 items deferred by user decision (branch protection → GitHub Pro/public; Clerk production → custom domain). See 02.5-HUMAN-UAT.md.
overrides_applied: 0
human_verification:
  - test: "Branch protection on main (D-04)"
    expected: "main requires the CI `verify` check to pass before merge"
    why_human: "GitHub API confirms the private repo is on a plan tier that returns 403 'Upgrade to GitHub Pro or make this repository public' for branch protection — requires a human plan/visibility decision, not a code fix"
  - test: "Provision Railway API + PostgreSQL (D-01/D-06/D-07/D-08/D-10)"
    expected: "API live on Railway; pre-deploy drizzle-kit migrate runs and passes before serve; GET /api/health returns {\"status\":\"ok\"}"
    why_human: "Requires Railway account auth, project/service creation, and real secret provisioning — cannot be automated by the verifier"
  - test: "Provision Vercel SPA (D-01/D-08/D-11)"
    expected: "Vercel builds apps/web with pnpm; SPA deep-links serve index.html; PR previews build"
    why_human: "Requires Vercel account auth and project import — cannot be automated by the verifier"
  - test: "Clerk production instance + cross-origin cutover (D-02/D-08/D-09)"
    expected: "Clerk prod instance live; allowed_origins include the Vercel domains; both hosts redeployed with cross-wired real URLs"
    why_human: "Requires Clerk dashboard access, DNS/CNAME setup, and live key issuance"
  - test: "Production cross-origin end-to-end smoke test"
    expected: "Health check, SPA deep-link, CORS allow/reject, authenticated login, and preview auth all pass against the live production deployment"
    why_human: "Requires a live deployed environment (real URLs) that does not yet exist; real-time browser/auth behavior cannot be verified by static code inspection"
---

# Phase 02.5: Deployment - CI/CD Pipeline and Production Deployment Setup Verification Report

**Phase Goal:** Establish the CI/CD pipeline and production deployment setup — a real CI gate (D-03), retired `db:push` in favor of `drizzle-kit migrate` for prod (D-05/D-06), env-driven CORS allowlist + Clerk `authorizedParties` (D-02/D-08/D-09), and deployment config for Railway (API+Postgres) + Vercel (SPA), plus the cross-origin production cutover (Clerk prod).
**Verified:** 2026-07-06T19:02:10Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Execution Context

Per an explicit mid-phase user decision ("artifacts now, provision later"), this phase deliberately split into two categories of work:

1. **Committable code + config artifacts** — produced, tested, merged to `main`, and pushed to `github.com/billy-ferris/ais-marketplace` (private). This is the scope this verifier can — and did — independently confirm against the live codebase and a real GitHub Actions run.
2. **Live cloud provisioning** (Railway/Vercel/Clerk account creation, secrets, branch protection, cross-origin cutover, production smoke test) — deliberately deferred to a human because it requires cloud-account authentication no agent can perform. These are tracked in `02.5-HUMAN-UAT.md` and are NOT code gaps.

All 11 artifact-scope must-haves (D-03, D-05, D-06, D-02/D-08/D-09 code, D-10, D-11 code) were independently re-verified against the actual repository (not just SUMMARY.md claims) — including running the full turbo gate, booting the production start command, exercising live CORS preflight requests, and re-running `drizzle-kit migrate` against a disposable Postgres database. All passed. The 5 remaining live-provisioning items are correctly classified as `human_needed`, not `gaps_found`.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-03: CI gate is real (eslint + tsc --noEmit + full API test suite) | VERIFIED | `pnpm turbo run typecheck lint test` independently re-run: 6/6 tasks successful, 178 API tests passed, no "No tasks were executed". `.github/workflows/ci.yml` runs `pnpm turbo run typecheck lint test` on push(main)+PR. |
| 2 | D-03: CI actually ran and passed on the real remote | VERIFIED | `gh run list -R billy-ferris/ais-marketplace`: run 28815691369 (`verify` job) — status `completed`, conclusion `success`, 55s, on `push` to `main`. |
| 3 | D-10: Production start boots from source, no ERR_MODULE_NOT_FOUND | VERIFIED | Independently booted `NODE_ENV=production pnpm --filter @ais/api start` on a free port with a validly-shaped dummy Clerk key — logged "API server running on port ####", `GET /api/health` → `{"status":"ok"}`. No ERR_MODULE_NOT_FOUND. `apps/api/package.json` `start` = `tsx src/index.ts`; `tsx`+`drizzle-kit` are in `dependencies` (not devDependencies). |
| 4 | D-05/D-06: db:push retired for prod; drizzle-kit migrate is the prod mechanism, applies clean + idempotent | VERIFIED | Independently ran `drizzle-kit migrate` against a fresh disposable Postgres DB (`ais_verify_spot_*`) — created 9 tables, no "relation already exists"; second consecutive run exited 0 with no changes (idempotent). Baseline `apps/api/drizzle/0000_petite_scarlet_witch.sql` + `meta/_journal.json` committed. `db:push` script still present in `apps/api/package.json` for dev only; `railway.toml preDeployCommand` runs migrate only (no seed/push). |
| 5 | D-02/D-09: CORS is an env-driven allowlist (static list + preview regex), rejects unknown origins, allows no-Origin requests | VERIFIED | `apps/api/src/lib/cors.ts` exports `staticAllowedOrigins()`/`buildCorsOptions()`; 7 unit tests in `origin.test.ts` all pass. Independently booted the API with `CORS_ALLOWED_ORIGINS=https://app.example.com` and confirmed live preflight: allowed origin → `Access-Control-Allow-Origin` header present; disallowed origin (`https://evil.example.com`) → no ACAO header. |
| 6 | D-08: Clerk authorizedParties bound to the same allowlist | VERIFIED | `apps/api/src/index.ts`: `clerkMiddleware({ authorizedParties: staticAllowedOrigins() })`, imported from `./lib/cors`. Same function feeds both `cors()` and Clerk. |
| 7 | D-08: FRONTEND_URL untouched (still single URL for email CTA links) | VERIFIED | `apps/api/.env.example` retains `FRONTEND_URL` unchanged with an explicit comment distinguishing it from `CORS_ALLOWED_ORIGINS`; not referenced in `cors.ts`. |
| 8 | D-01/D-10: Railway deploy config exists (Nixpacks build, pre-deploy migrate gate, tsx start, healthcheck) | VERIFIED | `railway.toml`: `builder = "NIXPACKS"`, `preDeployCommand = "pnpm --filter @ais/api exec drizzle-kit migrate"`, `startCommand = "pnpm --filter @ais/api exec tsx src/index.ts"`, `healthcheckPath = "/api/health"`, `restartPolicyType = "ON_FAILURE"`. |
| 9 | D-01/D-11: Vercel SPA config exists (rewrite for deep-linking) | VERIFIED | `vercel.json` — `{ "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }`, valid JSON (parsed successfully). |
| 10 | D-07: Env contract documented for both apps (no real secrets committed) | VERIFIED | `apps/api/.env.example` documents `CORS_ALLOWED_ORIGINS`/`CORS_PREVIEW_PATTERN`/D-08 prod notes; `apps/web/.env.example` documents `VITE_API_BASE_URL`/`VITE_CLERK_PUBLISHABLE_KEY` with dev/prod annotations. No live secret values present in either file (grep for TBD/FIXME/placeholder/secret patterns: none found). |
| 11 | No debt markers / stubs left in phase-modified files | VERIFIED | Scanned all key artifact files (eslint.config.js, turbo.json, package.json x4, cors.ts, origin.test.ts, index.ts, .env.example x2, ci.yml, railway.toml, vercel.json) for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER — zero matches. |

**Score:** 11/11 artifact-scope truths verified.

### Deferred to Human (Live Provisioning — Plan 07 + Task 2 checkpoints of Plans 03/05/06)

| # | Item | Locked Decision | Status |
|---|------|-----------------|--------|
| 1 | Branch protection on `main` | D-04 | BLOCKED on GitHub plan tier — confirmed via `gh api repos/.../branches/main/protection` → 403 "Upgrade to GitHub Pro or make this repository public". CI itself already runs on every push+PR regardless. |
| 2 | Railway API + Postgres provisioning | D-01/D-06/D-07/D-08/D-10 | Not yet provisioned — requires Railway account access. `railway.toml` is ready and correct. |
| 3 | Vercel SPA provisioning | D-01/D-08/D-11 | Not yet provisioned — requires Vercel account access. `vercel.json` is ready and correct. |
| 4 | Clerk production instance + cross-origin cutover | D-02/D-08/D-09 | Not yet provisioned — requires Clerk dashboard + DNS access. Code-side CORS/authorizedParties wiring is ready to consume the real values once set. |
| 5 | Production end-to-end smoke test | — | Cannot run — no live deployment exists yet (depends on items 2-4). |

These are correctly NOT classified as gaps — they are recorded in `02.5-HUMAN-UAT.md` (5 tests, all `pending`) and require human cloud-account action, not further code changes.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `eslint.config.js` | Flat eslint config, apps/* + packages/* | VERIFIED | Exists; `typescript-eslint` composed; verified via live `turbo run lint` (6/6 tasks pass). |
| `turbo.json` | `typecheck` task | VERIFIED | Present, `dependsOn: ["^build"]`. |
| `package.json` (root) | `engines.node: 22.x`, typecheck script, eslint deps | VERIFIED | All present. |
| `apps/api/package.json` | `start: tsx src/index.ts`; tsx/drizzle-kit in dependencies | VERIFIED | Confirmed via file read; confirmed functionally via live boot test. |
| `apps/api/drizzle/0000_petite_scarlet_witch.sql` + `meta/_journal.json` | Baseline migration | VERIFIED | Exists; independently applied to a fresh scratch DB (9 tables), idempotent re-run confirmed. |
| `apps/api/src/lib/cors.ts` | `buildCorsOptions`, `staticAllowedOrigins` | VERIFIED | Both exported; live preflight-tested. |
| `apps/api/src/__tests__/cors/origin.test.ts` | Unit test proving allow/reject | VERIFIED | 7 substantive tests (allow static/preview/no-origin, reject unknown/unset-preview, default localhost, credentials true) — all pass. |
| `apps/api/.env.example` | CORS_ALLOWED_ORIGINS + CORS_PREVIEW_PATTERN contract | VERIFIED | Present with explanatory comments; FRONTEND_URL preserved separately. |
| `.github/workflows/ci.yml` | GitHub Actions CI gate | VERIFIED | Present; independently confirmed live on GitHub (run 28815691369, conclusion success). |
| `railway.toml` | Nixpacks + pre-deploy migrate + tsx start + healthcheck | VERIFIED | All required keys present and correct. |
| `vercel.json` | SPA rewrite | VERIFIED | Valid JSON, catch-all rewrite present. |
| `apps/web/.env.example` | VITE_API_BASE_URL / VITE_CLERK_PUBLISHABLE_KEY documented | VERIFIED | Present, annotated dev/prod values, no secret key. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `turbo.json` typecheck/lint tasks | per-package scripts | `turbo run <task>` | WIRED | 6/6 tasks executed successfully across api/web/shared. |
| `apps/api` start script | tsx runtime | run-from-source | WIRED | Live-booted; confirmed no dist/build dependency. |
| `apps/api/src/db/schema/index.ts` | `apps/api/drizzle/0000_*.sql` | `drizzle-kit generate` | WIRED | Baseline SQL matches current schema tables (9 tables incl. Phase 02.4 partial unique indexes). |
| `drizzle-kit migrate` | Postgres `__drizzle_migrations` journal | applied-migration tracking | WIRED | Verified live: journal created, 1 entry, idempotent on re-run. |
| `.github/workflows/ci.yml` | turbo tasks | `pnpm turbo run typecheck lint test` | WIRED | Confirmed both in file and via live successful GitHub Actions run. |
| `apps/api/src/index.ts` | `apps/api/src/lib/cors.ts` | import + `app.use(cors(buildCorsOptions()))` | WIRED | Confirmed in source; confirmed functionally via live preflight test. |
| `staticAllowedOrigins` | Clerk `authorizedParties` | shared function | WIRED | Same import feeds both `cors()` and `clerkMiddleware()`. |
| `railway.toml preDeployCommand` | `drizzle-kit migrate` | separate pre-deploy container | WIRED (config) | Config present and correct; live Railway execution is a deferred human-provisioning item. |
| `railway.toml startCommand` | `tsx src/index.ts` | run-from-source prod start | WIRED (config) | Config present and correct. |
| Vercel build | `VITE_API_BASE_URL`/`VITE_CLERK_PUBLISHABLE_KEY` | build-time env injection | PARTIAL (config-ready, not yet live) | Env contract documented; actual Vercel build-time injection requires live provisioning (deferred). |
| Clerk prod `allowed_origins` | Vercel domains | dashboard allowlist | NOT_WIRED (deferred) | Clerk production instance does not exist yet — correctly deferred to human (Plan 07). |

### Behavioral Spot-Checks (independently re-executed by this verifier, not taken from SUMMARY claims)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full CI gate passes | `pnpm turbo run typecheck lint test` | 6 successful, 6 total; 178/178 API tests pass | PASS |
| Prod start boots, no ERR_MODULE_NOT_FOUND | `NODE_ENV=production pnpm --filter @ais/api start` + `curl /api/health` | "API server running on port ####"; `{"status":"ok"}` | PASS |
| CORS allows configured origin | preflight `OPTIONS` with `Origin: https://app.example.com` (CORS_ALLOWED_ORIGINS set to same) | `Access-Control-Allow-Origin: https://app.example.com` present | PASS |
| CORS rejects unknown origin | preflight `OPTIONS` with `Origin: https://evil.example.com` | No ACAO header | PASS |
| drizzle-kit migrate applies clean | `drizzle-kit migrate` against fresh scratch DB | 9 tables created, journal 1 entry, no error | PASS |
| drizzle-kit migrate is idempotent | second consecutive `drizzle-kit migrate` | exit 0, no changes, still 9 tables/1 journal entry | PASS |
| CI live on GitHub | `gh run list -R billy-ferris/ais-marketplace` | run 28815691369: `completed`/`success` | PASS |
| Branch protection blocked by plan tier (expected) | `gh api repos/.../branches/main/protection` | 403 "Upgrade to GitHub Pro or make this repository public" | CONFIRMED BLOCKED (matches HUMAN-UAT note; not a code gap) |

### Requirements Coverage

Infra phase — no product REQ-* IDs. Acceptance criteria are locked decisions D-01..D-11 (per `02.5-CONTEXT.md`), cross-referenced individually above.

| Decision | Description | Status | Evidence |
|----------|--------------|--------|----------|
| D-01 | Railway (API+PG) + Vercel (SPA) split hosting | Config VERIFIED / Live provisioning DEFERRED | `railway.toml` + `vercel.json` committed and correct; live services not yet created. |
| D-02 | Cross-origin deployment (CORS + Clerk allowlists agree) | Code VERIFIED / Cutover DEFERRED | `cors.ts` + `authorizedParties` wired and tested; real Vercel/Clerk origins not yet wired (Plan 07 deferred). |
| D-03 | CI: eslint + tsc --noEmit + full API test suite | SATISFIED | `.github/workflows/ci.yml` live and green on GitHub. |
| D-04 | Branch protection gates main | BLOCKED (human/plan-tier decision) | Confirmed 403 from GitHub API; requires Pro/public decision. |
| D-05 | Prod migrations via generated SQL, not db:push | SATISFIED | Baseline migration committed; `db:push` remains dev-only script. |
| D-06 | Migrate before serve, pre-deploy step | Config SATISFIED / Live gate DEFERRED | `railway.toml preDeployCommand`; live Railway execution pending provisioning. |
| D-07 | Secrets in host-native stores, `.env.example` is contract | SATISFIED (contract) / Real secrets DEFERRED | Both `.env.example` files documented; no real secrets committed anywhere. |
| D-08 | NODE_ENV=production; no TLS-reject flag in prod; VITE_* at build time | SATISFIED (code+docs) / Live enforcement DEFERRED | Guard exists in seed.ts (Phase 02.4); documented in both env.example files; live env-var setting pending provisioning. |
| D-09 | Env-var-driven CORS allowlist incl. preview pattern | SATISFIED | `cors.ts` + tests + live preflight confirm behavior. |
| D-10 | Nixpacks build, explicit start command | SATISFIED | `railway.toml [build] builder = "NIXPACKS"`; explicit `startCommand`. |
| D-11 | Vercel SPA rewrite + PR previews | Config SATISFIED / Previews-enabled DEFERRED (Vercel default, needs live project) | `vercel.json` rewrite present and valid; preview-deploy confirmation pending provisioning. |

### Anti-Patterns Found

None. Scanned all key files modified/created in this phase for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/"not yet implemented" — zero matches.

### Human Verification Required

See frontmatter `human_verification` section and `02.5-HUMAN-UAT.md` (5 tests, all currently `pending`, sequenced with documented dependencies: Railway → Vercel → Clerk → smoke test). Each requires live cloud-account authentication that cannot be automated by this verifier:

1. **Branch protection on `main` (D-04)** — Currently blocked by GitHub plan tier (private repo needs GitHub Pro, or make the repo public). CI already runs regardless.
2. **Provision Railway API + PostgreSQL (D-01/D-06/D-07/D-08/D-10)** — Create the service, add PG plugin, set real secrets, verify pre-deploy migrate gate and `/api/health` in live logs.
3. **Provision Vercel SPA (D-01/D-08/D-11)** — Import repo, confirm pnpm build, set build-time env vars, confirm deep-link rewrite + PR previews live.
4. **Clerk production instance + cross-origin cutover (Plan 07 Task 1)** — Create Clerk prod instance, set allowed_origins, cross-wire real URLs into both hosts, redeploy.
5. **Production cross-origin end-to-end smoke test (Plan 07 Task 2)** — Health check, deep-link, CORS allow/reject, authenticated login, and preview-auth checks against the live production deployment.

### Gaps Summary

No code or config gaps found. Every artifact this phase was responsible for producing exists, is substantive (not a stub), is correctly wired, and was independently re-verified by this verifier through direct execution (not by trusting SUMMARY.md claims) — including a live GitHub Actions run, a live production-mode API boot with functioning CORS enforcement, and an independent `drizzle-kit migrate` run against a disposable database.

The only unmet items are the five live cloud-provisioning steps that were explicitly and deliberately deferred to a human under the "artifacts now, provision later" decision, because they require account-level authentication to Railway, Vercel, Clerk, and GitHub (Pro/public repo decision) that no agent can perform. These are tracked in `02.5-HUMAN-UAT.md` and correctly drive the `human_needed` status rather than `gaps_found`.

---

*Verified: 2026-07-06T19:02:10Z*
*Verifier: Claude (gsd-verifier)*
