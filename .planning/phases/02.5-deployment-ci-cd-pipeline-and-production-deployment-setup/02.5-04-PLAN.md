---
phase: 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
plan: 04
type: execute
wave: 1
depends_on: ["02.5-01"]
files_modified:
  - apps/api/src/lib/cors.ts
  - apps/api/src/index.ts
  - apps/api/.env.example
  - apps/api/src/__tests__/cors/origin.test.ts
autonomous: true
requirements: [D-02, D-08, D-09]
must_haves:
  truths:
    - "CORS is an env-driven allowlist (CORS_ALLOWED_ORIGINS) plus an optional preview-URL regex (CORS_PREVIEW_PATTERN)"
    - "An allowed origin receives Access-Control-Allow-Origin; a disallowed origin does not"
    - "A no-origin request (curl/server-to-server) is allowed"
    - "clerkMiddleware has authorizedParties set to the same allowlist"
    - "FRONTEND_URL is left untouched (still a single URL used for email CTA links)"
  artifacts:
    - path: "apps/api/src/lib/cors.ts"
      provides: "Testable env-driven CORS origin function + shared allowlist"
      exports: ["buildCorsOptions", "staticAllowedOrigins"]
    - path: "apps/api/src/__tests__/cors/origin.test.ts"
      provides: "Unit test proving allow/reject behavior"
      contains: "describe"
    - path: "apps/api/.env.example"
      provides: "CORS_ALLOWED_ORIGINS + CORS_PREVIEW_PATTERN contract"
      contains: "CORS_ALLOWED_ORIGINS"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/lib/cors.ts"
      via: "import + app.use(cors(buildCorsOptions()))"
      pattern: "buildCorsOptions"
    - from: "staticAllowedOrigins"
      to: "clerkMiddleware authorizedParties"
      via: "single parsed list feeds both cors() and clerk"
      pattern: "authorizedParties"
---

<objective>
Replace the single-origin CORS config with an env-driven allowlist covering the Vercel production domain and the Vercel preview-URL pattern (D-09/D-02), and set Clerk `authorizedParties` to the same list (D-08 hardening). Extract the origin logic into a small testable helper so its allow/reject behavior is unit-tested.

Purpose: the split-host deploy (Vercel SPA → Railway API, cross-origin) requires the API to explicitly admit the Vercel origins and reject everything else; a permissive or single-origin CORS breaks previews and/or exposes the API.
Output: `apps/api/src/lib/cors.ts` helper, wired into `index.ts`, with `authorizedParties`, env contract, and a unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-RESEARCH.md
@.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-PATTERNS.md

<interfaces>
<!-- Executor uses these directly; no codebase exploration needed. -->
apps/api/src/index.ts (current CORS/Clerk block, lines 22-30):
  app.use(express.json());
  app.use(cors({ origin: process.env.FRONTEND_URL || 'http://localhost:5173', credentials: true }));
  app.use(clerkMiddleware());
Existing imports already present (index.ts lines 3-4): `import cors from 'cors'`, `import { clerkMiddleware } from '@clerk/express'`.
Webhook router is mounted BEFORE json/cors (line 20) and must stay first.
FRONTEND_URL is ALSO consumed by apps/api/src/services/email.ts (email CTA links) — do NOT overload it.
The SPA already sends `Authorization: Bearer <token>` (apps/web/src/lib/api.ts:66), so cross-origin token flow already works.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build + unit-test the env-driven CORS origin helper</name>
  <files>apps/api/src/lib/cors.ts, apps/api/src/__tests__/cors/origin.test.ts</files>
  <read_first>
    - RESEARCH.md §"Pattern 3: Env-driven CORS allowlist" lines 211-232 (origin-function shape; keep FRONTEND_URL separate; new vars CORS_ALLOWED_ORIGINS + CORS_PREVIEW_PATTERN) and §"Validation Architecture" line 454 (assertions to prove).
    - PATTERNS.md §"apps/api/src/__tests__/.../cors.test.ts" lines 124-151 (analogs: guard.test.ts env save/restore, error.test.ts callback-capture; assertions: allows static, allows preview, allows no-origin, rejects unknown) and §"Shared Patterns → Vitest unit test" lines 287-289.
    - Analog file `apps/api/src/__tests__/seed/guard.test.ts` (env save/restore + vi.restoreAllMocks in afterEach).
    - CONTEXT.md D-09 (env-driven allowlist; prod + preview) and D-02 (cross-origin).
  </read_first>
  <behavior>
    - Given CORS_ALLOWED_ORIGINS listing an origin, the origin function calls cb(null, true) for that origin.
    - Given CORS_PREVIEW_PATTERN matching an origin, it calls cb(null, true) for a preview URL.
    - Given no origin (undefined — curl/server-to-server/same-origin), it calls cb(null, true).
    - Given an origin not in the list and not matching the preview regex, it calls cb(err) (allow=false/undefined).
    - Defaults to http://localhost:5173 when CORS_ALLOWED_ORIGINS is unset.
  </behavior>
  <action>
    Create `apps/api/src/lib/cors.ts` exporting: (1) `staticAllowedOrigins()` — parse `process.env.CORS_ALLOWED_ORIGINS ?? 'http://localhost:5173'` by comma, trim, drop empties; (2) a `buildCorsOptions()` factory returning a `cors` options object whose `origin(origin, cb)` allows no-origin, allows any member of the static list, allows a match against `CORS_PREVIEW_PATTERN` (compiled to a RegExp only when set), and otherwise calls `cb(new Error(...))`; keep `credentials: true`. Follow RESEARCH Pattern 3's structure exactly (do not inline into index.ts). Write `apps/api/src/__tests__/cors/origin.test.ts` (Vitest) using the guard.test.ts env save/restore idiom for `CORS_ALLOWED_ORIGINS`/`CORS_PREVIEW_PATTERN` and a callback-capture helper (per PATTERNS lines 143-147) to assert the five behaviors above. No DB, no live server.
  </action>
  <verify>`pnpm --filter @ais/api test -- origin` — the CORS origin test passes (allows static, allows preview, allows no-origin, rejects unknown, honors default).</verify>
  <acceptance_criteria>
    - `apps/api/src/lib/cors.ts` exports the allowlist + a cors-options factory reading `CORS_ALLOWED_ORIGINS` and `CORS_PREVIEW_PATTERN`.
    - `origin.test.ts` proves: allowed static origin → allow; preview-pattern origin → allow; no origin → allow; unknown origin → reject; unset env → localhost default.
    - The full API suite still passes (`pnpm --filter @ais/api test`).
  </acceptance_criteria>
  <done>Testable CORS helper exists with passing unit coverage of allow/reject behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the allowlist into index.ts + Clerk authorizedParties + env contract</name>
  <files>apps/api/src/index.ts, apps/api/.env.example</files>
  <read_first>
    - RESEARCH.md §"Pattern 4: Clerk cross-origin hardening" lines 234-242 (authorizedParties = allowlist; production keys; Bearer flow already accepted) and §"Anti-Patterns" line 256 (do not overload FRONTEND_URL).
    - PATTERNS.md §"apps/api/src/index.ts — CORS block + clerkMiddleware" lines 154-190 (exact block to replace: lines 24-30; extract staticAllow once for both cors + clerk; preserve webhook-before-json ordering; no new imports) and §"apps/api/.env.example — CORS vars" lines 258-267.
    - CONTEXT.md D-08 (NODE_ENV=production; no TLS-reject in prod) and D-09.
    - Current `apps/api/.env.example` (lines 1-6: DATABASE_URL, PORT, CLERK_*, FRONTEND_URL).
  </read_first>
  <action>
    In `apps/api/src/index.ts`, replace the existing `cors({ origin: FRONTEND_URL ... })` block and the bare `clerkMiddleware()` call (lines 24-30) with: `app.use(cors(buildCorsOptions()))` and `app.use(clerkMiddleware({ authorizedParties: staticAllowedOrigins() }))`, importing both from `./lib/cors`. Preserve the webhook-router-before-json ordering (line 20) and the cors-before-clerk ordering — do not reorder. Do NOT touch `FRONTEND_URL` (it remains a single canonical URL for email CTA links). In `apps/api/.env.example`, add `CORS_ALLOWED_ORIGINS` (comma-separated, default `http://localhost:5173`) and `CORS_PREVIEW_PATTERN` (optional regex for Vercel preview URLs) with explanatory comments distinguishing them from `FRONTEND_URL`; add a comment noting prod sets `NODE_ENV=production` and must NOT set `NODE_TLS_REJECT_UNAUTHORIZED` (D-08). Do not add real secret values.
  </action>
  <verify>`pnpm --filter @ais/api test` passes; `grep -q 'authorizedParties' apps/api/src/index.ts`; `grep -q 'CORS_ALLOWED_ORIGINS' apps/api/.env.example`; boot the API with `CORS_ALLOWED_ORIGINS=https://app.example.com` and confirm a preflight from that origin returns an `Access-Control-Allow-Origin` header while a preflight from `https://evil.example.com` does not.</verify>
  <acceptance_criteria>
    - `apps/api/src/index.ts` uses `buildCorsOptions()` and `clerkMiddleware({ authorizedParties: staticAllowedOrigins() })`.
    - `FRONTEND_URL` still present/unchanged in code and env.example.
    - `apps/api/.env.example` documents `CORS_ALLOWED_ORIGINS` + `CORS_PREVIEW_PATTERN` and the NODE_ENV/no-TLS-reject prod notes.
    - Preflight from an allowed origin returns ACAO; from a disallowed origin returns no ACAO.
  </acceptance_criteria>
  <done>Cross-origin allowlist + Clerk authorizedParties are live in the API; env contract documents the new vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (any origin) → API | Cross-origin fetches from arbitrary origins hit the Railway API |
| SPA → Clerk-verified API | Session tokens presented as Bearer must originate from an authorized party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-025-04-CORS | Spoofing / Information Disclosure | Express `cors` origin function (`apps/api/src/lib/cors.ts`) | mitigate | Explicit env-driven allowlist + preview regex; reject unknown origins (no `*`, no reflect-any-origin). Unit-tested. |
| T-025-04-CLERK | Spoofing | `clerkMiddleware` token acceptance | mitigate | Set `authorizedParties` to the same allowlist so tokens from unauthorized frontends are rejected (prevents subdomain-cookie/CSRF). |
| T-025-04-I | Information Disclosure | overloading FRONTEND_URL into a CORS list | accept | Kept separate by design (email CTA links need a single URL); new CORS vars added additively. |
</threat_model>

<verification>
- CORS origin unit test passes (allow static/preview/no-origin, reject unknown).
- `index.ts` uses `buildCorsOptions()` + `authorizedParties`.
- Preflight admits allowed origins, rejects others; `.env.example` documents new vars.
</verification>

<success_criteria>
The API enforces an explicit cross-origin allowlist (prod + preview) and only accepts Clerk tokens from authorized parties — the D-02/D-08/D-09 cross-origin security posture is in code and tested, ready for the live origins wired in Plan 07.
</success_criteria>

<output>
Create `.planning/phases/02.5-deployment-ci-cd-pipeline-and-production-deployment-setup/02.5-04-SUMMARY.md` when done.
</output>
