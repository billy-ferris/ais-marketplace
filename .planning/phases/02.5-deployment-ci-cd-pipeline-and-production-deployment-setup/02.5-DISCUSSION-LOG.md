# Phase 02.5: Deployment — CI/CD Pipeline and Production Deployment Setup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-06
**Phase:** 02.5-deployment-ci-cd-pipeline-and-production-deployment-setup
**Areas discussed:** Hosting targets, CI pipeline scope, Prod DB migrations, Secrets & env mgmt, plus derived details (migration timing, CORS/Clerk origins, Railway build, Vercel SPA routing/previews)

---

## Hosting Targets

| Option | Description | Selected |
|--------|-------------|----------|
| All on Railway | API + Postgres + web static on one provider | |
| Railway API+DB, web on Vercel | Vercel SPA CDN + PR previews; two providers, cross-origin | ✓ |
| Railway API, Neon PG, web on CF Pages | Serverless PG + CF CDN; three providers | |

**User's choice:** Railway (API + Postgres), web on Vercel.
**Notes:** Introduces a cross-origin split — CORS + Clerk allowed-origins must cover the Vercel domain and preview URLs.

---

## CI Pipeline Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Lint + typecheck + tests, gate main | eslint + tsc + 171 API tests; branch protection on main | ✓ |
| Full gate + PR preview deploys | Above + ephemeral env per PR | |
| Test-only, no merge gate | CI reports but doesn't block | |

**User's choice:** Lint + typecheck + tests, gate merges to `main`.
**Notes:** No CI exists yet — greenfield GitHub Actions.

---

## Production DB Migrations

| Option | Description | Selected |
|--------|-------------|----------|
| Generated SQL migrations | db:generate → commit SQL → drizzle-kit migrate on deploy | ✓ |
| Keep db:push on deploy | Push schema straight to prod (destructive risk) | |
| Manual migrations, out-of-band | Apply SQL by hand at release | |

**User's choice:** Generated SQL migrations.
**Notes:** Retire `db:push` for prod (can drop/rename columns on live data). Keep push as a dev convenience.

---

## Secrets & Env Management

| Option | Description | Selected |
|--------|-------------|----------|
| Host env stores + .env.example contract | Real secrets in Railway/Vercel env UI; committed .env.example documents keys | ✓ |
| Encrypted .env in repo (dotenv-vault) | Single encrypted source, decrypted at deploy | |
| You decide | Claude picks per-host defaults | |

**User's choice:** Host env stores + `.env.example` contract.
**Notes:** Prod sets `NODE_ENV=production`, excludes dev-only `NODE_TLS_REJECT_UNAUTHORIZED`; web `VITE_*` injected at Vercel build time.

---

## Derived Details (all locked with recommended defaults)

| Detail | Locked decision |
|--------|-----------------|
| Migration timing | Railway release/pre-deploy step runs `drizzle-kit migrate` before the API starts serving |
| CORS + Clerk origins | Env-var-driven API CORS allowlist (prod Vercel domain + preview pattern), mirrored in Clerk allowed origins |
| Railway build method | Nixpacks auto-detect, explicit `pnpm --filter @ais/api start` (no Dockerfile unless needed) |
| Vercel SPA routing/previews | SPA rewrite (all routes → index.html) + PR previews enabled alongside GH Actions CI |

**User's choice:** Selected all four to lock; accepted the recommended default for each.

## Claude's Discretion

- Exact GitHub Actions YAML (job matrix, pnpm/turbo caching, concurrency), Railway config format (`railway.toml` vs dashboard), `vercel.json` specifics, and precise CORS env-var names.

## Deferred Ideas

- Monitoring / observability (error tracking, uptime, log aggregation, alerting).
- Autoscaling / performance tuning and preview-env cost controls.
- A dedicated staging environment (distinct from prod + PR previews).
